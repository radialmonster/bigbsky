2026-06-08: Attempted first automation run for "bigbsky.com work on plan.md". Could not read docs/plan.md, inspect git status, run tests, edit project files, commit, or push because every local process launch failed with `windows sandbox: runner error: CreateProcessAsUserW failed: 5`; Node REPL also exited with `windows sandbox failed: spawn setup refresh`. Runtime approx. a few minutes.
2026-06-08T21:??Z - Run blocked before repository inspection. PowerShell shell_command failed at process creation with `CreateProcessAsUserW failed: 5`; node_repl also exited before running file reads with `windows sandbox failed: spawn setup refresh`. Could not read `docs/plan.md`, inspect git status, edit files, verify, commit, or push.

2026-06-08T17:20:09.6380697-04:00 - Completed standalone thread unavailable-state polish. Future runs: use workspace `N:\Projects\bigbsky`; normal shell works with `workdir` set there and `login:false`. Read the plan with `Get-Content -LiteralPath docs\PLAN.md`, inspect status with `git status --short --branch`, list files with `rg --files`, and edit repo files under `N:\Projects\bigbsky` using `apply_patch`. This run added typed blocked/deleted/not-found/rate-limited/unavailable thread branch alerts in `src\App.tsx`, styling in `src\styles.css`, reader guard coverage in `scripts\verify-reader-behavior.mjs`, and updated `docs\PLAN.md`. Verified with `npm run build`.

Future-session prompt note for this automation:
Use workspace `N:\Projects\bigbsky`. The normal shell works when commands set `workdir` to `N:\Projects\bigbsky` and `login:false`. To inspect the run context, use `Get-Content -LiteralPath docs\PLAN.md`, `git status --short --branch`, and `rg --files`. Edit repo files under `N:\Projects\bigbsky` with `apply_patch`. After implementation, run `npm run build`, update `docs\PLAN.md`, commit, and push.
2026-06-08 blocked run

- Attempted required startup reads for memory, docs/PLAN.md, git status, and rg --files.
- Every shell invocation failed before process startup with: windows sandbox runner error: CreateProcessAsUserW failed: 5.
- No workspace files were inspected or changed, no build was run, and no git commit or push was attempted.
- A supplemental blocked-run note was written to memory.blocked-2026-06-08.md because shell-based memory access was unavailable.

2026-06-08T17:28:00-04:00 - Clarification after repeated blocked automation run: `CreateProcessAsUserW failed: 5` is an execution-environment/sandbox launch failure, not a repo path or command syntax issue. When the environment allows shell startup, use `workdir: N:\Projects\bigbsky` and `login:false`; those commands worked in the successful run. If the same CreateProcessAsUserW error appears before PowerShell starts, the session must be relaunched with an unrestricted/disabled filesystem permission profile or fixed process-launch sandbox before repo work can proceed.
2026-06-08T17:35:29.5096777-04:00 - Completed signed-in self-profile surface expansion. Changed src\App.tsx to replace the generic signed-in Profile cards with a dedicated self-profile account dashboard showing restored OAuth identity, account stats, sign-out, public-profile actions, and account-section cards for Posts, Replies, Media, Likes, Feeds, Starter Packs, and Lists. Updated src\styles.css with responsive self-profile account-card styling and updated docs\PLAN.md statuses. Verified with 
pm run build (tsc, Vite production build, static-output audit, reader behavior guard, layout behavior guard all passed; Vite still warns about a large lazy chunk). Committed and pushed 532e991 Expand signed-in profile surface. Next useful work: authenticated reads/API work for Likes, Starter Packs, account-backed Feed/List sync, or production OAuth callback verification.


2026-06-08T18:30:41.9334534-04:00 - Clarified sandbox diagnosis: do not assume the user changed anything when shell startup begins working after CreateProcessAsUserW failures. Treat it as an observed session/runtime recovery unless the environment explicitly reports a changed permission profile. No repo changes, build, commit, or push. Runtime approx. 1 minute.

2026-06-08T18:30:51.7964369-04:00 - Further clarification from user: the CreateProcessAsUserW failure recovered within the same session without user changes. Next automation prompt should treat it as a transient runner/process-launch failure first: retry startup reads once, then report blocked only if the same pre-PowerShell error repeats. No repo changes, build, commit, or push. Runtime approx. 1 minute.

2026-06-08T18:35:11.3827329-04:00 - Completed left-rail account switcher placement. Note: initial CreateProcessAsUserW failures recovered in-session; do not infer the user changed sandbox permissions unless explicitly reported. Changed src\App.tsx to make the signed-in rail avatar open a compact account switcher with restored identity, Open profile, Account settings, Sign out, and an add/switch OAuth form. Added scoped switcher styling in src\styles.css and updated docs\PLAN.md to mark sign-out/account-switcher placement improved while account-backed multi-account sync remains pending. Verified with npm run build (tsc, Vite production build, static-output audit, reader behavior guard, layout behavior guard all passed; Vite still warns about a large lazy chunk). Committed a48d0bc Add left-rail account switcher and pushed to origin/main successfully. Worktree clean after push. Next useful work: production OAuth callback/metadata verification when deploy/domain state is available, authenticated reads for notifications/lists/likes, or live browser stress testing of several-page virtualization.

2026-06-08T18:42:27.8122108-04:00 - Completed standalone thread parent/reply context. Treat earlier CreateProcessAsUserW failures as transient runner issues when they recover without user action. Changed src\api.ts to retain AppView thread parent nodes, src\App.tsx to collect and render parent posts as a compact reply-context ancestor chain above the opened standalone post, src\styles.css with scoped thread-context styling, and docs\PLAN.md to mark thread parent/reply context implemented for standalone routes. Verified with npm run build (tsc, Vite production build, static-output audit, reader behavior guard, layout behavior guard all passed; Vite still warns about a large lazy chunk). Committed ba52404 Add thread parent context and pushed to origin/main successfully. Worktree clean after push. Next useful work: authenticated reads for notifications/lists/likes, production OAuth callback/metadata verification when deploy/domain state is available, or live browser stress testing of several-page virtualization.

2026-06-08T18:47:09-04:00 - Prompt update requested after interruption: startup CreateProcessAsUserW failure handling should say to retry the required startup reads once immediately, and if the environment is later relaunched or reports unrestricted/disabled filesystem permissions, continue the automation by running the startup reads again instead of treating the prior blocked result as final. Do not infer a repo/path/npm/git issue from pre-PowerShell CreateProcessAsUserW failures. No repo changes, build, commit, or push in this prompt-update turn.

2026-06-08T18:47:30-04:00 - User asked for the whole updated automation prompt. Provided full prompt text with clarified CreateProcessAsUserW retry/relaunch behavior. No repo files changed, no build, commit, or push.

2026-06-08T18:48:04-04:00 - User requested stronger retry language. Updated proposed whole prompt to require one retry for required startup reads if commands fail, with special handling for pre-PowerShell CreateProcessAsUserW failures and normal diagnosis only after retry. No repo files changed, no build, commit, or push.

2026-06-08T18:48:22-04:00 - User clarified they wanted the whole updated prompt with stronger retry behavior included inline. Provided complete replacement prompt. No repo files changed, no build, commit, or push.

2026-06-08T18:53:51.8952773-04:00 - Completed AppView video/GIF embed playback-card polish. Important correction: do not describe recovered shell access as "now unrestricted" or imply the user changed anything; observed fact is that the pre-PowerShell CreateProcessAsUserW runner failure recovered and commands launched successfully. Changed src\App.tsx to replace linked video thumbnails with a reusable VideoEmbedCard that uses native browser video controls for playlist URLs, keeps thumbnail fallback, exposes alt text, and offers Open media for the source playlist. Updated src\styles.css so native video elements share stable media sizing in normal, media-heavy, compact, and quoted-post contexts. Updated docs\PLAN.md to mark media/GIF/video rendering improved while deferring richer cross-browser HLS playback unless a small client library is justified. Verified with npm run build (tsc, Vite production build, static-output audit, reader behavior guard, layout behavior guard all passed; Vite still warns about a large lazy chunk). Committed db121a2 Improve video embed playback cards and pushed to origin/main successfully. Worktree clean after push. Runtime approx. 5 minutes. Next useful work: authenticated reads for notifications/lists/likes, production OAuth callback/metadata verification when deploy/domain state is available, or live browser stress testing of several-page virtualization.

2026-06-08T19:04:42-04:00 - Improved record-with-media rendering and local surface scroll restoration. Investigated live post https://bigbsky.com/profile/dara-the-explara.bsky.social/post/3mnsj3miim223 via public AppView: root `record.text` is empty, but embed is `app.bsky.embed.recordWithMedia#view` with one image under `embed.media.images` and a quoted post under `embed.record.record`; prior helpers only recognized direct images/direct record URI, causing the fallback "Post has no plain text." Updated `src\api.ts` helpers to read recordWithMedia media images/external/video and unwrap nested quote records. Also updated `src\App.tsx` to preserve route-specific SPA scroll offsets for browser-local Saved and Lists timelines, expanded `scripts\verify-reader-behavior.mjs` coverage, and updated `docs\PLAN.md`. Verified `npx tsc -b`, `node scripts\verify-reader-behavior.mjs`, `node scripts\verify-layout-behavior.mjs`, and a live Node fetch shape check for the Dara post. Full `npm run build` failed twice after TypeScript at Vite config loading with esbuild `Error: spawn EPERM`; esbuild binary itself reports version 0.25.12. Commit/push blocked: `git add`/`git commit` failed twice with `fatal: Unable to create 'N:/Projects/bigbsky/.git/index.lock': Permission denied`; no stale lock or Git process was present. No commit hash created and push not attempted. Worktree has edits in `src\api.ts`, `src\App.tsx`, `scripts\verify-reader-behavior.mjs`, and `docs\PLAN.md`. Runtime approx. 9 minutes. Next run: retry `npm run build`, then `git add`, commit, and push if the transient process/filesystem permission issues clear.

2026-06-08T19:08:18-04:00 - Retried after environment reported unrestricted filesystem access. Required startup reads succeeded. `npm run build` passed fully (tsc, Vite production build, static-output audit, reader behavior guard, layout behavior guard; Vite still warns about a large lazy chunk). Committed `785181b` (`Support record-with-media cards`) and pushed to `origin/main` successfully. Worktree clean after push. Changes shipped: `src\api.ts` supports AppView `recordWithMedia` media/quote unwrapping so empty-text quote/image cards like Dara's post render rich content instead of the no-text fallback; `src\App.tsx` restores SPA scroll offsets for browser-local Saved/List timelines; `scripts\verify-reader-behavior.mjs` guards both behaviors; `docs\PLAN.md` status updated. Runtime approx. 3 minutes for retry turn. Next useful work: production deploy verification of the fixed live card and OAuth/domain callback checks when deploy completes.

2026-06-08T19:14:27.3900695-04:00 - Implemented reload-aware timeline scroll restoration but did not commit because full 
pm run build remains blocked by Node child_process spawn EPERM inside Vite/esbuild. Changed src\App.tsx to persist feed/profile/saved/list scroll offsets in browser sessionStorage under igbsky:timeline-scroll, flush on pagehide, restore offsets after freshly loaded feed/profile data and cached surface revisits, and clear igbsky:* sessionStorage keys from Settings local data reset. Updated scripts\verify-reader-behavior.mjs to guard reload scroll persistence and session cleanup. Updated docs\PLAN.md status for scroll restoration. Verified 
px tsc -b, 
ode scripts\verify-reader-behavior.mjs, and 
ode scripts\verify-layout-behavior.mjs all passed; direct esbuild binary version command from PowerShell returned 0.25.12. Full 
pm run build failed three times at Vite config loading with Error: spawn EPERM; 
px vite build --configLoader runner also failed with spawn EPERM; Node spawnSync checks for node.exe, cmd.exe, and esbuild.exe all returned EPERM, confirming a session/process-launch blocker rather than app code. No commit hash created and push not attempted. Worktree has edits in src\App.tsx, scripts\verify-reader-behavior.mjs, and docs\PLAN.md. Runtime approx. 18 minutes. Next run: retry full 
pm run build; if it passes, commit and push the scroll restoration changes.

2026-06-08 (later) - Picked up the other loop's in-progress reload-aware scroll restoration. That loop had implemented it but couldn't commit because its session hit Vite/esbuild spawn EPERM on the full `npm run build`. In THIS session the process-launch blocker was absent: full `npm run build` passed clean (tsc, vite, build audit, reader + layout guards; only the usual large lazy-chunk warning). Committed `80299d1` (Persist timeline scroll across browser reloads) and pushed to origin/main (785181b..80299d1). Files: src\App.tsx (sessionStorage persistence of feed/profile/saved/list scroll offsets, flush on pagehide, restore after reload/fresh loads, clears bigbsky:* session storage on local data reset), scripts\verify-reader-behavior.mjs (guards for reload persistence + cleanup), docs\PLAN.md (scroll status updated). Note: spawn EPERM on npm run build is a per-session process-launch issue, not a code problem - if a run hits it, the work may still be correct; retry in a fresh session before declaring blocked.

2026-06-08 (later 2) - Completed live Feed discovery on the Explore surface. Selected the `/explore` placeholder gap (plan: "Discover new Feeds, suggested Feed cards"). Changes: src\api.ts adds getPopularFeedGenerators (public app.bsky.unspecced.getPopularFeedGenerators, verified 200 + Access-Control-Allow-Origin:* unauthenticated). src\App.tsx: memoized activeSource into a useMemo that builds a synthetic FeedSource for any `at://` Feed URI not in the static feedSources list (so /feed/:uri now opens arbitrary public Feeds in-app; the existing getFeedGenerator effect still fetches real metadata for the right-rail panel), and added an ExploreDiscoverFeeds component that fetches 12 popular Feeds and renders cards (avatar, name, creator, description, like count) that open in-app via onOpenFeed plus an Open-on-Bluesky link. src\styles.css: .discover-feeds* card grid styling. docs\PLAN.md: updated /feed/:uri, /explore, and the Phase 3 menu-destination status lines. NOTE on the synthetic-source memo: activeSource MUST stay memoized (deps [route, activeSourceId]) - it was previously a stable feedSources reference; a fresh object each render would make the route->loadFeed effect (App.tsx ~882) refetch every render. Verified with full `npm run build` (tsc, vite, audit 87kB gzip JS / 7kB CSS, reader guard, layout guard all passed; only the usual large lazy-chunk warning). Committed 8cc583d "Add live Feed discovery to Explore" and pushed 80299d1..8cc583d to origin/main; worktree clean after push. Runtime approx. 6 minutes. Next useful work: pin integration for discovered Feeds (pinnedFeedIds currently resolves only against static feedSources, so discovered-Feed pins won't show in the selector - would need the selector/pinnedSources to resolve arbitrary at:// URIs), authenticated reads (notifications/lists/likes), or production OAuth/deploy verification.

2026-06-08 (later 3) - Completed pin integration for discovered Feeds (the next-work item flagged after the Explore Feed discovery run). Problem fixed: discovered public Feeds use arbitrary at:// generator URIs that are not in the static feedSources list; pinnedFeedIds resolved only against feedSources, and readPinnedFeedIds actively dropped non-static ids on reload, so discovered-Feed pins never appeared in the selector and vanished after refresh. Changes: src\App.tsx adds pinnedFeedMetaStorageKey ("bigbsky:pinned-feed-meta"), isPinnedFeedMeta validation, readPinnedFeedMeta(), readPinnedFeedIds(meta) now keeps ids known to either static OR meta sources, new pinnedFeedMeta state, pinnedSources memo resolves ids against feedSources then pinnedFeedMeta (deps [pinnedFeedIds, pinnedFeedMeta]), togglePinnedFeed syncs the meta store for non-static sources, the Pinned group filter now matches discovered pins on search query, and ExploreDiscoverFeeds builds one reusable FeedSource per card with a local Pin/Unpin button (receives pinnedFeedIds + onTogglePinnedFeed from SurfaceView). src\styles.css adds .discover-feed-actions/.discover-feed-pin styling (uses --bsky-blue-soft; note: there is no --app-link var in this project). scripts\verify-reader-behavior.mjs gained 4 guards for the meta store key, readPinnedFeedMeta validation, combined knownIds resolution, and togglePinnedFeed meta sync. docs\PLAN.md status lines updated (Phase 3 pinned/custom feeds, /explore route, Phase 5 pinned feeds). Note: the Settings clear-local-data path already removes all bigbsky:* keys so the new meta key is covered automatically. Verified with full npm run build (tsc, vite 87kB gzip JS / 7kB CSS, build audit, reader guard, layout guard all passed; only the usual large lazy-chunk warning). Committed ad7f7c0 "Pin discovered Feeds from Explore" and pushed 8cc583d..ad7f7c0 to origin/main; worktree clean after push. IMPORTANT for next run: I initially ran the startup Get-Content reads through the Bash tool (POSIX bash) which failed with "Get-Content: command not found" - use the PowerShell tool (not Bash) for PowerShell cmdlets. No CreateProcessAsUserW errors this run. Runtime approx. 7 minutes. Next useful work: authenticated reads (notifications/lists/likes), production OAuth/deploy verification, or live browser stress testing of several-page virtualization.

2026-06-08 (loop) - Completed live Trending Topics on the Explore surface (next Explore-parity gap after Feed discovery/pin work). Verified app.bsky.unspecced.getTrendingTopics returns 200 + Access-Control-Allow-Origin:* unauthenticated; response has {topics, suggested} arrays where each item is {topic, link}. Changes: src\api.ts adds TrendingTopic/TrendingTopicsResponse types and getTrendingTopics(limit, signal) on public.api.bsky.app. src\App.tsx adds ExploreTrendingTopics component (fetches 14 topics, merges topics+suggested, renders Hash-icon chips) rendered above ExploreDiscoverFeeds on /explore; chips open an in-app post search via a new onOpenSearchQuery prop wired to submitSearch (added to SurfaceView props/type alongside the existing onOpenSearch). src\styles.css adds .trending-topics/.trending-topic-chip pill styling. scripts\verify-reader-behavior.mjs guards the component + api reader. docs\PLAN.md updated (/explore route line + "Search and trending topics" Phase 3 line). Decision: suggested-accounts discovery stays deferred because app.bsky.actor.getSuggestions returns {"actors":[]} unauthenticated. Note: trending kept scoped to Explore (a discovery surface) rather than replacing the right-rail loaded-post hashtag trends, to avoid adding a network call on every surface (request-budget caution). Verified full npm run build (tsc, vite 87kB gzip JS / 8kB CSS, audit, reader guard, layout guard all passed; usual large lazy-chunk warning only). Committed 6e88e79 "Add live trending topics to Explore" and pushed ad7f7c0..6e88e79 to origin/main; worktree clean. This run was driven by a /loop (10m session cron 019b668c). Next useful work: live Feed search via getPopularFeedGenerators query param (the Search "Feeds" tab + Explore could accept a query), authenticated reads (notifications/lists/likes), or production OAuth/deploy verification.

2026-06-08 (loop 2) - Completed live public Feed search in the Search "Feeds" tab (the candidate flagged after trending topics). Previously the Feeds tab only filtered static local feedSources. Changes: src\api.ts getPopularFeedGenerators now takes an optional 3rd arg query (signature (limit, signal, query) to avoid breaking the existing ExploreDiscoverFeeds call) and forwards it as the `query` param (verified endpoint returns relevant 200 results for ?query=). src\App.tsx adds FeedSearchState type + emptyFeedSearchState + feedSearchState state + feedSearchCacheRef + loadFeedSearch useCallback (caches per `feeds:<query>` key, getPopularFeedGenerators(20, signal, query)); the route-driven search effect now has a searchTab==="feeds" branch calling loadFeedSearch(route.query) so it only fires after explicit /search?q= navigation (not per keystroke); feedSearchState is reset in clearSearch, the cache-clear block, and the other tab branches; SearchView gained a feedSearchState prop/type; the Feeds tab now renders a "Local Feed destinations" section plus a "Public Feeds on Bluesky" live section with loading/empty/error/rate-limit states, opening results in-app via the synthetic Feed source. src\styles.css adds .search-section-heading. scripts\verify-reader-behavior.mjs guards the feeds branch + loader. docs\PLAN.md "Search result view" line updated to "improved". Verified full npm run build (tsc, vite 87kB gzip JS / 8kB CSS, audit, reader guard, layout guard all pass; usual large lazy-chunk warning only). Committed f483456 "Add live public Feed search to the Feeds tab" and pushed 6e88e79..f483456 to origin/main; worktree clean. Driven by /loop (10m session cron 019b668c). Next useful work: Explore "Discover New Feeds" could also accept a query (reuse the new query param); load-more/pagination for feed search (cursor exists); authenticated reads (notifications/lists/likes); or production OAuth/deploy verification.

2026-06-08 (loop 3) - Added a submit-only Feed search box to Explore's "Discover New Feeds" section (reusing the getPopularFeedGenerators query param added last loop). src\App.tsx ExploreDiscoverFeeds now holds draftQuery + activeQuery state; the fetch effect keys on [activeQuery] and calls getPopularFeedGenerators(18, signal, activeQuery); a form with a Search input commits draftQuery->activeQuery on submit (no per-keystroke fetch), plus an X clear button and a query-aware empty state. src\styles.css adds .discover-feeds-search/.discover-feeds-clear pill styling. scripts\verify-reader-behavior.mjs guards submit-only refetch + the [activeQuery] effect. docs\PLAN.md /explore line updated. Verified full npm run build (tsc, vite 88kB gzip JS / 8kB CSS, audit, reader guard, layout guard all pass; usual large lazy-chunk warning). Committed 7e8c21c "Add Feed search box to Explore discovery" and pushed f483456..7e8c21c to origin/main; worktree clean. Driven by /loop (10m session cron 019b668c). Recent loop trio all targeted Feed discovery/search parity (pin meta -> trending -> feed-search tab -> explore feed search box). Next useful work candidates: feed-search load-more/pagination (PopularFeedsResponse.cursor exists but neither Explore nor the Feeds tab paginate); the /feeds directory page still only lists static known Feeds (could host pinned + discover + search like a real Feeds destination, plan line 121); authenticated reads (notifications/lists/likes); or production OAuth/deploy verification. Consider rotating away from Feed-discovery features next loop to avoid over-investing one area.

2026-06-08 (loop 4) - Rotated from Feed-discovery to PROFILE surfaces: added a live "Feeds" tab to public profiles listing the Feeds that account published. Verified app.bsky.feed.getActorFeeds returns 200 unauthenticated. Changes: src\api.ts adds getActorFeeds(actor, limit, signal) reusing PopularFeedsResponse. src\App.tsx: profileTabs now includes "feeds"; new ProfileFeedsTab component fetches getActorFeeds(actor, 50) on actor change with loading/error/rate-limit/empty states; the profile render branches so profileTab==="feeds" shows ProfileFeedsTab instead of the post VirtualPostList. REFACTOR: extracted a shared DiscoverFeedCard component (avatar/body/likes/pin/open-on-bluesky) now used by both ExploreDiscoverFeeds and ProfileFeedsTab, and added a reusable openFeedSource(source) helper (setActiveSourceId+remember+navigate) replacing 3 duplicated inline feed-open handlers (feed selector onClick, SurfaceView onOpenFeed, profile-route SurfaceView... actually the search-route SurfaceView). Note left at App.tsx ~line 1432 there is still one more setActiveSourceId usage I deliberately left (it predates the openFeedSource const definition; TDZ-safe to leave). scripts\verify-reader-behavior.mjs guards profileTabs feeds entry, ProfileFeedsTab+getActorFeeds, the feeds-tab render branch, and the api reader. docs\PLAN.md "Profile view variants" line updated. Verified full npm run build (tsc, vite 88kB gzip JS / 8kB CSS, audit, reader guard, layout guard all pass; usual large lazy-chunk warning). Committed dea8847 "Add a Feeds tab to public profiles" and pushed 7e8c21c..dea8847 to origin/main; worktree clean. Driven by /loop (10m session cron 019b668c). Next useful work candidates: profile Lists tab via app.bsky.graph.getLists(actor) (verified 200, returns lists/cursor - empty for bsky.app but works); the self-profile Feeds account-card could now link to the live getActorFeeds for the signed-in DID; feed-search pagination; or authenticated reads. Keep rotating areas.

2026-06-08 (loop 5) - Added a live "Lists" tab to public profiles (continuing profile-surface work after the Feeds tab). Verified app.bsky.graph.getLists returns 200 + CORS * unauthenticated (empty for accounts with no lists; pfrazee.com has a real one). Changes: src\api.ts adds ListView + ActorListsResponse types and getActorLists(actor, limit, signal). src\App.tsx: profileTabs now ["posts","replies","media","videos","feeds","lists"]; new ProfileListsTab component + listPurposeLabel helper (modlist->Moderation list, curatelist->User list); profile render branch adds profileTab==="lists" -> <ProfileListsTab actor=.../>. List cards reuse .discover-feed-card markup (List icon glyph, name, "<type> · N members", description) and link Open on Bluesky (no in-app list reader yet). scripts\verify-reader-behavior.mjs guards lists tab + getActorLists; ALSO had to loosen the prior Feeds-tab guard regex (it anchored ...,"feeds"\] which broke once "lists" was appended - changed to drop the trailing \]). docs\PLAN.md "Profile view variants" line updated and added a forward note on "Personal feeds/lists" for a future in-app list-timeline reader via app.bsky.feed.getListFeed. Verified full npm run build (tsc, vite 88kB gzip JS / 8kB CSS, audit, reader guard, layout guard all pass; usual large lazy-chunk warning). First build attempt failed ONLY on the stale Feeds-tab guard regex (expected, fixed). Committed e0fbf8d "Add a Lists tab to public profiles" and pushed dea8847..e0fbf8d to origin/main; worktree clean. Driven by /loop (10m session cron 019b668c). LESSON: when guarding an array literal in verify scripts, don't anchor the closing bracket - future appends will break it. Next useful work candidates: in-app Bluesky list-timeline reader via getListFeed (new source type/route - bigger); wire self-profile Feeds/Lists account cards to live getActorFeeds/getActorLists for the signed-in DID; feed-search pagination; authenticated reads. Consider rotating away from profiles next loop (3 of last 5 loops touched feeds/profiles discovery).

2026-06-08 (loop 6) - Rotated to THREAD/POST surface: added on-demand liked-by/reposted-by/quotes panels to the standalone post view (plan line 135 parity). Verified app.bsky.feed.getLikes/getRepostedBy/getQuotes all return 200 unauthenticated on a live post (an old/deleted post can 500 on getRepostedBy - test against a current post). Shapes: getLikes -> {likes:[{actor:Profile}]}, getRepostedBy -> {repostedBy:Profile[]}, getQuotes -> {posts:FeedPost[]}. Changes: src\api.ts adds LikesResponse/RepostedByResponse/QuotesResponse types + getLikes/getRepostedBy/getQuotes(uri, limit, signal). src\App.tsx: ThreadView gained engagement state (null|"reposts"|"quotes"|"likes"); the Reposts/Quotes/Likes <dd> values are now .thread-stat-button toggles (disabled when count is 0) that set engagement; new ThreadEngagementPanel component fetches the matching endpoint ONLY when expanded (respects the no-prefetch thread budget), rendering reposted-by/liked-by as .profile-result-card actor rows (reusing Avatar) -> onOpenProfile, and quotes as cards -> onOpenPost; panel has loading/error/rate-limit/empty states + close button. src\styles.css adds .thread-stat-button + .thread-engagement* styles. scripts\verify-reader-behavior.mjs guards the panel + the toggle + the 3 api readers. docs\PLAN.md "Post/thread detail view" line updated to "improved". Verified full npm run build (tsc, vite 89kB gzip JS / 8kB CSS, audit, reader guard, layout guard all pass; usual large lazy-chunk warning). Committed 766ea8d "Add liked-by/reposted-by/quotes panels to threads" and pushed e0fbf8d..766ea8d to origin/main; worktree clean. Driven by /loop (10m session cron 019b668c). Area rotation so far: feed-discovery x4 -> profiles x2 -> thread x1. Next useful work candidates: pagination/load-more for the engagement panels (cursor exists) and for profile feeds/lists tabs; in-app Bluesky list-timeline reader via getListFeed; authenticated reads; or production OAuth/deploy verification.

2026-06-08 (loop 7) - Rotated to POST TEXT rendering: post body now renders Bluesky rich-text facets (links + @mentions clickable). Previously post text was plain {text}. Changes: src\api.ts adds RichTextFacet type and record.facets field on FeedPost. src\App.tsx adds renderRichText(text, facets, onOpenProfile) helper (byte-offset aware via TextEncoder/TextDecoder - facets use UTF-8 byte offsets, NOT char offsets; also note facet offsets index into RAW post.record.text not the trimmed display text, so the renderer uses raw text when facets exist). link facets -> external <a class=post-link> with stopPropagation; mention facets -> <button class=post-mention> calling onOpenProfile({did, handle}) where handle = segment minus leading @. Unknown/tag facets fall through to plain text. PostCard post-text <p> now calls renderRichText(facets?.length ? raw : trimmed, facets, onOpenProfile). Imported ReactNode type. src\styles.css adds .post-link/.post-mention (bsky-blue-soft, underline on hover). scripts\verify-reader-behavior.mjs guards the renderer + its usage. docs\PLAN.md line 109 (Link cards...) updated. DEFERRED: clickable #hashtag->in-app-search, because it needs a new search callback threaded through ~6 PostCard call sites + VirtualPostList + the handlers object (too much plumbing for one loop); noted in plan + here as a follow-up. Verified full npm run build (tsc, vite 89kB gzip JS / 8kB CSS, audit, reader guard, layout guard all pass; usual large lazy-chunk warning). Committed 939a42b "Render link and mention facets in post text" and pushed 766ea8d..939a42b to origin/main; worktree clean. Driven by /loop (10m session cron 019b668c). Area rotation: feed-discovery x4 -> profiles x2 -> thread x1 -> post-text x1. Next candidates: clickable hashtag->search (thread an onOpenTag/search callback through PostCard/VirtualPostList); apply renderRichText to quote-post text too (RecordEmbedView.value has no facets currently though); pagination/load-more for engagement panels + profile feeds/lists; in-app list-timeline reader (getListFeed); authenticated reads.

2026-06-08 (loop 8) - Completed the rich-text facet feature: #hashtags in post text now open an in-app post search (links + mentions shipped last loop). Key design choice: rather than thread a new onOpenTag callback through ~6 PostCard call sites + VirtualPostList + the ThreadView handlers object, added a lightweight module-level TagSearchContext (createContext<((tag:string)=>void)|null>) that ONLY PostCard consumes via useContext; the App return is wrapped in <TagSearchContext.Provider value={openTag}> (provider added around the app-shell div: opening after `return (`, closing before `);` at the imageViewer/</div> anchor). openTag(tag) = submitSearch(tag.startsWith("#")?tag:`#${tag}`); defined just above submitSearch (TDZ-safe, only called from click handlers). renderRichText gained a 4th param onOpenTag and a app.bsky.richtext.facet#tag branch (-> <button class=post-tag>, uses feature.tag or segment minus #). src\styles.css adds .post-tag. scripts\verify-reader-behavior.mjs: extended the renderRichText guard to require the #tag branch + a TagSearchContext guard; ALSO had to loosen last loop's exact renderRichText(...) call-signature guard (it pinned the closing paren after onOpenProfile, which broke when onOpenTag was appended - same anchored-arg lesson as the profileTabs array). docs\PLAN.md line 109 updated (deferral removed). Verified full npm run build (tsc + vite confirm the context wrap compiles; audit 89kB gzip JS / 8kB CSS; reader+layout guards pass; usual large lazy-chunk warning). First build failed ONLY on the stale call-signature guard (expected, fixed). Committed 9e25b55 "Make post hashtags open an in-app search" and pushed 939a42b..9e25b55 to origin/main; worktree clean. Driven by /loop (10m session cron 019b668c). LESSON (recurring): verify-script regexes that anchor the END of an arg list or array literal break whenever an arg/element is appended - prefer matching a prefix, don't anchor the trailing )/]. Area rotation: feed-discovery x4 -> profiles x2 -> thread x1 -> post-text x2. Next candidates: pagination/load-more for engagement panels + profile feeds/lists (cursors exist); in-app list-timeline reader via getListFeed; apply renderRichText to quote-post/thread reply text (RecordEmbedView.value currently lacks facets in our type - would need to add); authenticated reads; production OAuth/deploy verification. Consider rotating to a fresh area (search/right-rail/notifications-local) next.

2026-06-08 (loop 9) - First committed the user's pending right-rail changes (commit c97956c: clear-X on right-rail search box, Clear button on Recent panel header via new clearRecentItems, and Recent detail text wraps to 2 clamped lines). Then implemented the in-app Bluesky LIST-TIMELINE READER (the follow-up noted in loops 5/8). Verified app.bsky.feed.getListFeed and app.bsky.graph.getList both 200 unauthenticated. Changes: src\api.ts adds getListFeed(list,cursor,signal)->FeedResponse, getList(list,signal)->{list:ListView}, and isListUri(uri) (matches "/app.bsky.graph.list/"). src\App.tsx: loadFeed branches isListUri(source.uri)?getListFeed:getFeed (both return {feed,cursor}); activeSource memo labels list URIs "List"; new listMetadata state + listMetadataCacheRef; the feed-generator metadata effect now branches - for list URIs it fetches getList (skipping getFeedGenerator which would 400) and stores listMetadata, else getFeedGenerator as before; FeedContextPanel takes listMetadata and renders list-aware (List glyph, name/desc/avatar/creator, "Members" instead of "Likes", Type=List, correct /lists/ bsky URL, "Pin/Unpin list" labels); ProfileListsTab takes onOpenFeed and makes CURATED lists (purpose includes curatelist) open in-app (card button + "Open list" action) while modlists keep only Open-on-Bluesky (no readable timeline); listMetadataCacheRef cleared in clearLocalReaderData. scripts\verify-reader-behavior.mjs guards the getListFeed branch + curate-list open + the 3 api helpers. docs\PLAN.md updated: /feed/:uri route line, Profile Lists line, and "Personal feeds/lists" note. Verified full npm run build (tsc, vite 90kB gzip JS / 8kB CSS, audit, reader+layout guards all pass; usual large lazy-chunk warning). Committed 6fb4ea7 "Read Bluesky list timelines in-app" and pushed c97956c..6fb4ea7 to origin/main; worktree clean. Driven by /loop (10m session cron 019b668c). Pinned-list note: pinning a list stores its at:// uri in pinnedFeedMeta like a feed, so it reopens the list timeline via /feed/:uri. Area rotation: feed-discovery x4 -> profiles x2 -> thread x1 -> post-text x2 -> right-rail(user) -> lists-reader. Next candidates: pagination/load-more for engagement panels + profile feeds/lists + list timeline (cursors exist); facets in quote-post text (RecordEmbedView.value lacks facets in our type); authenticated reads; production OAuth/deploy verification.

2026-06-08 (fix) - User reported lazy-load/pagination failing "after a while especially for searches" with "Unable to load / Failed to fetch". ROOT CAUSE: every loader (loadFeed/loadProfileFeed/loadSearch/loadActorSearch) catch block set status to "error" regardless of whether it was an initial load or a load-MORE. Since the loaded posts render only inside the `status === "ready"` block, a single failed pagination request replaced the ENTIRE loaded list with the full ErrorState screen - losing everything scrolled. Search hits it most because api.bsky.app search rate-limits sooner, and a rate-limited response without CORS headers surfaces as a fetch TypeError "Failed to fetch". FIX (src\App.tsx): added loadMoreError?:string to FeedState/SearchState/ActorSearchState; each catch now branches on `cursor` - if paginating, keep results (status stays "ready", set loadMoreError) instead of wiping; initial loads behave as before. AutoLoadMoreButton gained an `error` prop: when set it STOPS the IntersectionObserver auto-fire (prevents retry storms against a rate-limited endpoint) and renders an inline message + manual Retry button. Added isNetworkError(TypeError) + friendlier rateLimitMessage (429 / network / generic). All 4 AutoLoadMoreButton call sites pass error={...loadMoreError}. src\styles.css adds .load-more-error. scripts\verify-reader-behavior.mjs guards the keep-results-on-pagination-failure branch + the auto-loader pause-on-error. docs\PLAN.md two perf lines updated. Verified full npm run build (tsc, vite 90kB gzip JS / 8kB CSS, audit, reader+layout guards pass). Committed c5b856e "Keep results when pagination fails; stop retry storms" and pushed 6fb4ea7..c5b856e to origin/main; worktree clean. Note: this was a user bug report, not a loop turn. Follow-up idea: bounded exponential backoff + auto-retry-once for transient network errors (plan still mentions backoff); engagement panels / profile feeds-lists / list timeline still lack load-more pagination (cursors exist).

2026-06-08 (loop 10) - First committed the user's pending composer-gating change (commit 6ed8514: the inline "new post" Composer at the top of the feed now only renders when authState.session exists; signed-out visitors see the timeline start directly with posts; plan line 955 updated). Then for the loop item, completed facet coverage: QUOTED-POST text now renders rich-text facets (links/mentions/hashtags clickable) - previously only top-level post text did. Changes: src\api.ts adds facets?:RichTextFacet[] to RecordEmbedView.value. src\App.tsx QuotedPostCard reads TagSearchContext via useContext and renders text with renderRichText(record.value.facets?.length ? raw : trimmed, facets, onOpenProfile, onOpenTag) - same pattern as PostCard. scripts\verify-reader-behavior.mjs guards the quote-facet render. docs\PLAN.md line 109 facet note extended. Verified full npm run build (tsc, vite 90kB gzip JS / 8kB CSS, audit, reader+layout guards pass; usual large lazy-chunk warning). Committed 74d37a7 "Render rich-text facets in quoted posts" and pushed 6ed8514..74d37a7 to origin/main; worktree clean. Driven by /loop (10m session cron 019b668c). Area rotation: feed-discovery x4 -> profiles x2 -> thread x1 -> post-text x2 -> right-rail(user) -> lists-reader -> pagination-fix(user bug) -> composer-gating(user) -> quote-facets. Next candidates: load-more pagination for engagement panels / profile feeds-lists / list timeline (cursors exist); bounded exponential backoff + auto-retry-once for transient network errors (plan still calls for backoff); live right-rail trending via getTrendingTopics (currently loaded-post-derived); authenticated reads; production OAuth/deploy verification.

2026-06-08 (loop 11) - Rotated to MODERATION/safety: gated adult/graphic media behind a click-to-reveal content warning (plan lines 139/1024). Previously sensitive-labeled posts showed images/video directly with only a text chip in the moderation notice. Changes: src\App.tsx - added EyeOff to lucide imports; in PostCard added mediaRevealed state, mediaWarningLabels = sensitiveLabels minus "spam" (spam isn't about imagery), and gateMedia = mediaWarningLabels.length>0 && (images||video) && !mediaRevealed; wrapped the images-grid + VideoEmbedCard blocks so when gateMedia they're replaced by a .sensitive-media-gate button (EyeOff, "Sensitive content", label list, "Show") that sets mediaRevealed=true; after reveal a .sensitive-media-hide button can re-cover. Thumbnails aren't even in the DOM until revealed (privacy/bandwidth win). src\styles.css adds .sensitive-media-gate/.sensitive-media-show/.sensitive-media-hide. scripts\verify-reader-behavior.mjs guards gateMedia + the Show gate. docs\PLAN.md lines 1023 (media/labels) + 1024 (muted/blocked) updated. Verified full npm run build (tsc, vite 90kB gzip JS / 8kB CSS, audit, reader+layout guards pass; usual large lazy-chunk warning). Committed 0557da7 "Gate adult/graphic media behind a content warning" and pushed 74d37a7..0557da7 to origin/main; worktree clean. Driven by /loop (10m session cron 019b668c). Note: gate applies to top-level post media only; quote-card embedded media is not yet gated (sensitive labels live on the outer post). Area rotation: discovery x4 -> profiles x2 -> thread -> post-text x2 -> right-rail -> lists-reader -> pagination-fix -> composer-gating -> quote-facets -> sensitive-media. Next candidates: gate quote-card media too; load-more pagination for engagement/profile-feeds-lists/list-timeline; bounded backoff/auto-retry-once; live right-rail trending; authenticated reads; production OAuth/deploy verification.

2026-06-08 (loop 12) - Completed the NSFW gate: quoted-post media is now gated too (previously only top-level post media was, so a labeled post quoted by another showed ungated images/video). Refactor: extracted sensitiveMediaValues(labels) helper (filter isSensitiveLabel, lowercase val, drop spam, dedupe) and a shared <SensitiveMediaGate values onReveal> component (EyeOff + "Sensitive content" + label list + Show); both PostCard and QuotedPostCard use them. QuotedPostCard now reads ShowNsfwContext + has mediaRevealed state and computes mediaWarningValues from record.labels (cast Array<{val?}>) + record.author?.labels; gateMedia wraps the embeddedImages grid + embeddedVideo. scripts\verify-reader-behavior.mjs updated: PostCard gate guard now matches <SensitiveMediaGate.../>, added a quote-gate guard. docs\PLAN.md media/labels line notes quote media + shared gate. Verified full npm run build (tsc, vite 91kB gzip JS / 8kB CSS, audit, reader+layout guards pass; usual large lazy-chunk warning). Committed 79fa7f9 "Gate sensitive media in quoted posts too" and pushed 5a635a3..79fa7f9 to origin/main; worktree clean. Driven by /loop (10m session cron 019b668c). Context on NSFW: confirmed via live api that porn/sexual labels come back unauthenticated on BOTH post.labels and post.author.labels; truly-unlabeled adult content can't be detected client-side. Area rotation (lots of moderation lately): discovery x4 -> profiles x2 -> thread -> post-text x2 -> right-rail -> lists-reader -> pagination-fix -> composer-gating -> quote-facets -> sensitive-media x3 (gate, toggle, author-labels, quote-gate). NEXT: rotate AWAY from moderation. Candidates: load-more pagination for engagement panels / profile feeds-lists / list timeline (cursors exist); bounded backoff/auto-retry-once; live right-rail trending via getTrendingTopics; authenticated reads; production OAuth/deploy verification.

2026-06-08 (loop 13) - Rotated away from moderation to the RIGHT RAIL: replaced the loaded-post-derived right-rail "Trending" with live trending. New TrendingPanel component (placed before FeedMapPanel) fetches getTrendingTopics(10) ONCE on mount - the right rail is part of the persistent App shell so it mounts once per session, not per route (addresses the earlier request-budget concern about per-surface fetches). Renders live topics (topic.topic clickable -> onOpenTopic=submitSearch -> in-app post search); falls back to the loaded-post hashtag trends (fallback prop = existing trendingTopics memo), then static #atproto/#bluesky/#socialweb if live fetch is empty/fails. Replaced the inline <section> Trending block in the right rail with <TrendingPanel fallback={trendingTopics} onOpenTopic={submitSearch} />. getTrendingTopics/TrendingTopic already imported (from Explore work). scripts\verify-reader-behavior.mjs guards TrendingPanel + fallback. docs\PLAN.md "Search and trending topics" line -> improved. Verified full npm run build (tsc, vite 91kB gzip JS / 8kB CSS, audit, reader+layout guards pass; usual large lazy-chunk warning). Committed 4bb4184 "Use live trending in the right-rail panel" and pushed 79fa7f9..4bb4184 to origin/main; worktree clean. Driven by /loop (10m session cron 019b668c). Area rotation: discovery x4 -> profiles x2 -> thread -> post-text x2 -> right-rail(user) -> lists-reader -> pagination-fix -> composer-gating -> quote-facets -> moderation x3 -> right-rail-trending. Next candidates: load-more pagination for engagement panels / profile feeds-lists / list timeline (cursors exist); bounded backoff/auto-retry-once for transient net errors; authenticated reads (notifications/lists/likes); production OAuth/deploy verification; consider live right-rail trending could ALSO surface the suggested feeds (response.suggested are feed links).
2026-07-06T22:58:28-04:00 - Completed M8 BlueskyListCard viewer-state re-sync from todo.md. Startup reads succeeded without CreateProcessAsUserW failures. Existing local commit 6ebf60a was already ahead of origin/main; left it intact and pushed it with this run. Changed src\App.tsx so BlueskyListCard re-seeds blockUri and muted from refreshed list.viewer state in a useEffect keyed by list.uri/list.viewer?.blocked/list.viewer?.muted. Added a reader-behavior verifier guard in scripts\verify-reader-behavior.mjs and marked M8 done in todo.md. Verified npm run build passed (tsc, Vite, static audit, reader/layout/rich-text verifiers). Committed b7cb50a "fix: resync Bluesky list viewer controls" and pushed main 762d57d..b7cb50a to origin successfully, including prior local commit 6ebf60a. Worktree clean after push. Runtime approx. 6 minutes. Next useful items: M10 PostComposer insertAtCaret stale closure, M4/M6 OAuth session cleanup hardening, M11/M12 load-more/profile reload race guards, or L12 ListMemberManager remove busy guard.

2026-07-31T23:39:53-04:00 - Completed M14 + L14 + L21 from todo.md and relocated run memory into the repo. IMPORTANT: memory.md now lives at N:\Projects\bigbsky\memory.md (in-repo, committed) instead of C:\Users\Owner\.codex\automations\... — the nextsessionprompt.md startup-read line was updated to `Get-Content -LiteralPath memory.md`. Startup reads succeeded; no CreateProcessAsUserW failures. Changes: (M14) added the app's first toast primitive — ToastContext (createContext<(message, kind?: ToastKind)=>void>), ToastMessage/ToastKind types, a ToastHost fixed overlay owned by App (rendered inside .app-shell after ImageViewer, z-index 100, role=alert for errors / role=status otherwise, X dismiss button), pushToast/dismissToast callbacks in App (capped at 4 visible, 6s auto-dismiss), and the ToastContext.Provider wrapping the whole App render tree (outermost, around TagSearchContext.Provider). toggleFollowFeed now pushes an actionable error toast on catch ("Couldn't follow/unfollow this Feed. Please try again.") instead of console.error only. (L14) TrendingPanel renders a subtle .trending-note under its h2 when state.status === "error" so default trends aren't mistaken for live ones. (L21) dropped the no-op scripts/audit-build.mjs entry from tsconfig include (allowJs:false means .mjs never type-checked anyway). Styles added: .trending-panel .trending-note + .toast-host/.toast/.toast-error/.toast-success/.toast-dismiss. Also refreshed nextsessionprompt.md with a forward-looking "WHAT TO WORK ON THIS SESSION" batch (L17 SW base paths, L18 CSP with inline-script hash note, M4/M6 OAuth cleanup, H1 narrower ErrorBoundary, bookmarks scroll-restore via content anchoring, engagement/list pagination, search NSFW parity, App.tsx decomposition, reuse toast for other silent failures). Verified: npx tsc -b clean; npm test 202/202 green (12 files); npm run build fully green (tsc, vite 153kB gzip initial JS / 14kB CSS, audit, reader + layout + rich-text verifiers). Live CDP smoke on a fresh dev server: app boots clean on 127.0.0.1:5173 (app-shell + left rail render), TrendingPanel live-renders (no note, healthy), no console errors; built CSS contains .toast-host + .trending-note; compiled App.tsx module contains ToastHost + the toast strings. NOTE: follow-failure toast could NOT be exercised live — it requires signedInDid (OAuth), which isn't available on the localhost origin in a fresh profile; the code path is trivial and tsc/build-verified. Committed b7074b8 "feat: shared toast system; surface follow/trending failures" (code + todo + nextsessionprompt.md). This memory entry + hash recorded in a follow-up commit; pushed to origin/main along with the 3 pre-existing unpushed local commits (d528b85, fb56ac9, 15e50db). Dev server (port 5173) + CDP Chrome (9222) started for verification and killed at session end. Next useful work: L17/L18 (SW paths + CSP), M4/M6 OAuth hardening, H1 subtree ErrorBoundary, engagement/list load-more pagination, bookmarks scroll-restore content-anchoring, search NSFW filtering parity.


