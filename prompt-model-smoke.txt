Log the start time immediately with `Get-Date -Format o`.

You are running a Bigbsky model/provider smoke test in the current repo/worktree.

Goal:
- Verify this model can run a loop session, use the shell, and read basic repo context.
- Do not modify files.
- Do not create, edit, label, comment on, close, or otherwise mutate GitHub Issues, Pull Requests, Discussions, or Releases.
- Do not run package installs, tests, builds, deploys, or long commands.

Run only these checks:

```powershell
Get-Date -Format o
Get-Location
git status --short --branch
gh --version
gh auth status --hostname github.com
```

Then output a JSON object on a single line followed by a human-readable summary. The JSON must appear first and exactly as shown — no markdown fences, no extra text before it:

{"smoke_result":"pass","path":"...","git_branch":"...","git_clean":true,"gh_available":true,"gh_auth_usable":true,"start_time":"...","end_time":"..."}

Use "pass" or "fail" for smoke_result. Then add a brief human-readable summary below the JSON line.

Stop after this one smoke check.
