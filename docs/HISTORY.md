Done in the 2026-08-02 decomposition slice 24 (issues #18/#19): extracted the VirtualPostList measurement math out of src/App.tsx into a pure lib module src/lib/virtual-list.ts (estimateRowHeight per density, overscanPixelsFor, findRowIndexByOffset binary search, computeRowOffsets, totalRowHeight, isAboveViewport) with a 14-case unit suite src/lib/virtual-list.test.ts. VirtualPostList now imports these helpers; App.tsx dropped ~40 lines of inline binary-search/offset math and the scroll-compensation condition reads through isAboveViewport. Retired four layout regex guardrails in scripts/verify-layout-behavior.mjs (the defaultRowHeight density ternary, the `defaultRowHeight * 3` overscan, the inline `while (low <= high)` row lookup, and the `rowTop + previousHeight <= container.scrollTop` compensation condition - their guarantees now live in the lib suite). Remaining regex tally: 22 in verify-reader-behavior.mjs + 37 in verify-layout-behavior.mjs. Also closed #20: re-ran the CSS zero-usage cross-check after the media/thread-view extractions and found zero dead selectors (the only zero-hit pair, toast-error/toast-success, is the dynamic toast-${kind} build). Full gate green: vitest 585 tests / 58 files, tsc -b, npm run build (audit + all verifiers); live smoke-verified via dev server + CDP (feed renders .virtual-list with data-total-rows/data-rendered-rows, wheel-scroll + thread navigation + back-navigation work, zero horizontal overflow, zero console errors).

Done in the 2026-08-01 decomposition slice 20 (#18/#19, unblocked by #7 closing): extracted VideoEmbedCard (plus its videoKindLabel helper and the VideoEmbedView type) out of src/App.tsx into src/features/post/VideoEmbedCard.tsx - the first PostCard/media-cluster piece. All moved verbatim and imported back; App dropped its now-unused Film lucide import. Added 6 behavioral tests (#19) in src/features/post/VideoEmbedCard.test.tsx: Bluesky aspect-ratio metadata lands on the stable card frame (--video-aspect inline style), no ratio leaves the frame unconstrained, compact adds the quote-video-card class, GIF vs Video label, thumbnail-fallback link, and the no-playlist/no-thumbnail placeholder. Retired two layout regex guardrails in scripts/verify-layout-behavior.mjs (the video.aspectRatio metadata pin and the videoFrameStyle --video-aspect pin - their guarantees now live in the component suite); the image.aspectRatio pin stays until PostImageVideoMedia moves. Remaining regex tally: 43 in verify-reader-behavior.mjs + 42 in verify-layout-behavior.mjs; App.tsx now 7,038 lines. Full gate green: vitest 529 tests / 54 files, tsc -b, npm run build (audit + all verifiers); live smoke-verified via dev server + CDP (shell + 5 feed post cards, zero horizontal overflow, zero console errors).

Done in the 2026-08-01 #7 token step (this session, third batch): completed the media-sizing tokenization of src/styles.css and pinned it in scripts/verify-layout-behavior.mjs. New :root tokens: --media-max-height (calc(100vh - 140px), the viewport-capped embed height used by .image-grid img, .video-card, .post-image-video-media .image-row, and .image-row-solo .image-button img), --media-only-max-height (calc(100vh - 132px), the media-only-surface cap used by .media-only-single img, .media-only-row, and .media-only-card video/img/.video-placeholder), --video-min-height (180px, the reserved playback frame on .video-card/.video-card video/.video-placeholder), and --link-card-thumb (clamp(220px, 24%, 440px), the link-card thumbnail column). All values byte-identical to the prior literals; no visual or behavior change. The layout verifier now requires the token block and the token-bearing .video-card rule and forbids re-introducing the literals at call sites (max-height: calc(100vh - 140px/132px), min-height: 180px, grid-template-columns: clamp(...)). Verified live via dev server + CDP at 1920x1080 (video-card min 180px / max 845px at a 985px viewport, link-card grid 220px/402px, shell grid 76/288/1220/320) and 390x844 (single column, no overflow), zero console errors. Full gate green: vitest 523 tests / 53 files, tsc -b, npm run build (audit + all verifiers). #7 CLOSED; the remaining value-change audit (the riskier, evidence-gated half) is tracked in #52.

Done in the 2026-08-01 #7 token step (this session): introduced the second batch of identical-value semantic tokens in src/styles.css and pinned them in scripts/verify-layout-behavior.mjs. New :root tokens: --thread-indent (22px, drives the --thread-depth * 22px indent math at the three .thread-node/.thread-alert/.thread-continuation call sites), --timeline-padding-block/-inline/-bottom (22px/26px/48px, the .timeline/.thread-view scroll-surface padding), and --card-min-narrow/medium/wide/xwide (280/340/360/400px, the five surface grids: info-grid, surface-grid/feed-directory-grid, settings-grid, self-profile-tabs, discover-feeds-grid). All values byte-identical to the prior literals; no visual or behavior change. The layout verifier now requires the token block and the token-bearing rules and forbids re-introducing the literals (22px multiply, 22px 26px 48px padding, and minmax(min(100%, 280|340|360|400px))). Verified live via dev server + CDP at 1920x1080 (shell grid 76/288/1236/320px, timeline padding 22/26/48px, depth-2 thread node 44px indent) and 390x844 (single column, no overflow), plus a scroll-restore smoke (feed back-nav restored ~1178). Full gate green: vitest 523 tests / 53 files, tsc -b, npm run build (audit + all verifiers). #7 stays OPEN for the remaining riskier tokenization (virtualized feed/media sizing, value changes) and the mega-stylesheet split.

Done in this pass: minimized + enumerated OAuth scopes (no misleading "Chat" on consent), single-source scope (JSON), Chat + Edit-profile delegate to bsky.app, account Follow write, post Like write, account Block/unblock write (profile-header control + a per-post 3-dots "More options" menu on feed cards — both confirm-on-block, seeded from `viewer.blocking`, shared via a DID-keyed BlockContext so blocking reflects across every post by that author; verified live end-to-end against a real blocked account), authenticated reads for viewer-state everywhere (author-feed, post-thread/branches, and post search now route through the signed-in agent so Like/Follow/Block initial state is correct on every surface), blocked-profile handling (the same single profile viewer now loads the header independently of the author feed via `allSettled` so a blocked account's failed/empty feed no longer collapses the header to "Unknown user" — the Unblock control stays reachable — and the posts area shows a clear "You've blocked this account" notice instead of the misleading "No posts in this tab"), real post + reply publishing (composer "Post"/"Post All" publishes `app.bsky.feed.post` records — multi-post drafts chain into a thread sharing the first post as root; the inline thread reply box publishes a reply with the correct root/parent ref; both detect rich-text facets via `RichText.detectFacets` so links/@mentions/#hashtags render, keep the 300-char validation, and refresh the feed/thread after posting; image attachments still post text-only with an inline note until the separate image-upload task lands), "Permissions updated" re-auth prompt (on restore, `getMissingScopes()` compares the session's granted scope from `getTokenInfo()` against the desired `OAUTH_SCOPE` as token sets — the stored grant format is byte-identical so no false positives — and a dismissible non-modal banner at the top of the main column offers one-click re-authorize via `startSignIn(handle)`; dismissal is remembered per missing-scope signature so a newly-added scope re-prompts but the same gap doesn't nag; ALSO: because dismissing the global banner is permanent per-signature, the surfaces that actually break on a missing scope now self-explain — the Notifications view and the list mute/block toggles detect a scope-related failure via `getMissingScopes()` and show an inline "Update permissions" re-authorize button so dismissing the top banner never strands a feature), real image attachments on posts/replies/threads (file picker → `agent.uploadBlob` → `app.bsky.embed.images`, capped at 4/post per the lexicon, with preview thumbnails, per-image alt text, and a remove control; images are session-only since File objects can't persist, and object URLs are revoked on remove/clear/post/unmount), "Project" feed-group cleanup (investigated: the `"Project"` label was only applied to transient descriptors for dynamically-opened public feeds/lists and was never rendered as a selector group or shown anywhere user-facing — confirmed no empty "Project" group exists; renamed to the clearer "Discovered" at all four producer sites + the `FeedSource` type, keeping `"Project"` accepted in the pinned-meta validator as a legacy alias so existing pins still load), real Lists page (the `/lists` route now surfaces the signed-in user's actual Bluesky lists via `getMyLists()` — owned lists from `app.bsky.graph.getLists` for their DID plus curation lists they subscribe to, read from `savedFeedsPrefV2` `type:"list"` items and resolved with `getList`; cards show purpose/avatar/member-count, curation lists open as a timeline through the existing `getListFeed` route, and each links out to Bluesky; the old browser-local list "workspaces" are demoted to a collapsed "Browser collections" section that still backs the post-card Lists control; signed-out shows a sign-in prompt; mod-list subscriptions still need `getListMutes`/`getListBlocks` scopes and are out of scope here), stale-copy sweep (refreshed the user-visible surface posture cards and self-profile cards that still said "OAuth later / Pending / staged / waits for OAuth" — Explore/Feeds/Profile/Saved intro copy + cards and the self-profile Likes/Feeds/Lists cards now describe the shipped reality: signed-in reads with viewer-state, feed follow/unfollow, real Bluesky lists, and local Saved/collections), build & curate block lists (the Lists page can create a moderation list `app.bsky.graph.list` (modlist purpose), manage members inline on owned lists via `getListMembers` + add-by-handle `app.bsky.graph.listitem` + per-member remove + delete-list, and subscribe/unsubscribe to someone else's block list via `app.bsky.graph.listblock` seeded from the list's `viewer.blocked`; all writes were already in scope — only *mute*-list subscribe remains, needing a `muteActorList` scope add. NOTE: this bumped the audit's initial-JS gzip guardrail 100→110 kB; the structural fix is to code-split write-heavy authenticated surfaces, tracked under Performance Architecture), real Notifications view (the `/notifications` route now renders the signed-in user's actual Bluesky notifications via `getNotifications` (`listNotifications`) — likes/reposts/follows/replies/mentions/quotes with author avatar, action text, post-text preview, and relative time; All vs Mentions tabs; unread rows highlighted; clicking opens the related post (`reasonSubject` for like/repost → your post; the notification uri for reply/mention/quote) or profile (follow); pagination via cursor; marks seen on view with `updateSeen`; the old browser reader summary is demoted to a collapsed `<details>`), mute-list subscribe (the block-list subscribe toggle on others' moderation lists now has a sibling "Mute list"/"Unmute list" control calling `muteActorList`/`unmuteActorList`, seeded from `viewer.muted`, confirm-guarded — completing the block+mute list subscription pair; required adding the `muteActorList`/`unmuteActorList` scopes), configurable Home page (Settings → Home Page lets the user pick what the house icon / `bigbsky.com` root opens — Following (default), Discover, Bluesky Team, or any subscribed feed when signed in; stored locally under `bigbsky:home-source`; `resolveHomeSource()` falls back to public Discover whenever the chosen home needs a session that isn't present, so Home never breaks if auth is lost or during the signed-out public visit; root "/" with no feed uri and the Home nav both resolve through it), native Bookmarks (replaced the browser-local `bigbsky:saved-posts` list with Bluesky's native bookmark feature, and renamed the whole surface from "Save/Saved" to "Bookmark/Bookmarked" + the nav item/route from `Saved`/`/saved` to `Bookmarks`/`/bookmarks` to match bsky.app — the per-post Bookmark/Bookmarked control now writes through the authenticated AppView via `app.bsky.bookmark.createBookmark`/`deleteBookmark` (auth.ts `bookmarkPost`/`unbookmarkPost`), seeded from `post.viewer.bookmarked`, optimistic with revert-on-error, shared via a `SaveContext` consumed inside `PostCard` so bookmark state survives row virtualization and is consistent across every surface without prop-threading; the `/bookmarks` route now lists the account's real bookmarks via `app.bsky.bookmark.getBookmarks` (`getBookmarks` in auth.ts maps each `#bookmarkView.item` postView into a feed item, with cursor "Load more"), signed-out shows a sign-in prompt, and bookmarks sync with bsky.app; Settings/Notifications/Profile copy updated to say bookmarks live in the Bluesky account; required adding the three `bookmark.*` rpc scopes — bookmarks are AppView-private like mutes, so they are `rpc:` not `repo:`. The bookmark namespace + `viewer.bookmarked` are present in the installed `@atproto/api`. The authenticated write/read path cannot run in the localhost-only Claude preview (OAuth is rejected there); build/tsc verify wiring, so end-to-end bookmark/list needs confirming on the deployed origin after re-consent), profile replies-only empty-state, left-rail tooltip readability, removed Feed Map and Build Posture panels.

Done in the 2026-06-10 bug-fix + cleanup pass (code-review of the whole `src` tree): fixed the Like control rendering a notification-bell icon instead of a heart (now `Heart`; `Bell` stays for the Notifications nav); added auth-change cache invalidation (the nine feed/profile/list/thread/search cache refs plus the like/bookmark/block optimistic overrides are dropped whenever the signed-in DID changes, so viewer state can't go stale across sign-in/out — skipped on first mount, and the feed loader's `AbortController` prevents an in-flight public fetch from repopulating); reconciled the image cap to 10 in the UI; **corrected during deployed verification:** 5–10 image posts require the new `app.bsky.embed.gallery` embed, while `app.bsky.embed.images` is still lexicon-capped at 4, so the write path now uses `embed.images` for 1–4 and `embed.gallery` for 5–10; guarded `ImageViewer` against an all-srcless embed (no-op at the open site + `return null` defense); wired the timeline scroll container ref into `BookmarksView` and through `SurfaceView` → `ListsSurface` so scroll restoration on `/bookmarks` and `/lists` actually works (the reader-behavior verifier already required both keys); gave sign-out its own `"signing-out"` auth status instead of reusing `"signing-in"`. Cleanup: removed the dead `mediaSlots` composer-draft field (always written `{}`, never read — images are session-only), 5 stale `SurfaceView` props left over from the `BookmarksView` split (`currentDid`/`onOpenImage`/`onOpenPost`/`onOpenLinkPreview`/`onToggleListPost`), the unused `getProfile` import (the public read is reached via the dynamic import in `getProfileAuthed`), the unused `readWorkspaceWidthPreference` helper, and the unused `LayoutList` icon import; and enabled `noUnusedLocals`/`noUnusedParameters` in `tsconfig.json` so the build (`tsc -b`) now blocks unused-symbol regressions. `tsc`, the build audit (initial JS 103 kB gzip), and both reader/layout behavior verifiers all pass; none of it was exercised in a real browser (see the operator runtime-verify task above).

Done in the 2026-08-01 decomposition slice (issues #18/#19): extracted the first leaf component cluster out of `src/App.tsx` into `src/features/feed/AutoLoadMoreButton.tsx` — `AutoLoadMoreButton`, `PostRowFallback`, and `findScrollParent` moved verbatim and imported back into App.tsx (no behavior change; live smoke-verified via dev server + CDP: shell renders, feed loads 58 rows, `.load-more` button present, no console errors). This proves the #18 extraction pattern: component moved into `src/features/**`, CSS deliberately NOT co-located yet because `src/styles.css` carries in-flight #7 CSS-token edits — do the mega-stylesheet split after #7 lands. Ported the matching regex source-text guardrails to a real behavioral test (#19): added `src/features/feed/AutoLoadMoreButton.test.tsx` (React Testing Library, 4 tests: explicit-click load, no auto-fire on mount, error+Retry surface, row-fallback alert) and removed the two now-dead regex assertions from `scripts/verify-reader-behavior.mjs` (the IntersectionObserver auto-loader guard and the PostRowFallback alert rule — they matched App.tsx source text that no longer lives there). Added `@testing-library/react` as a devDependency. Full gate green: vitest 218 tests / 13 files, `tsc --noEmit`, `npm run build` (static-output audit + all verifiers).

Done in the 2026-08-01 decomposition slice 2 (issues #18/#19): extracted the leaf/state cluster — `Avatar`, `LoadingState`, `ErrorState`, `SensitiveMediaGate`, `MediaHiddenButton`, `ReplyLimitedNotice` — out of `src/App.tsx` into `src/features/common/Avatar.tsx`, `src/features/common/State.tsx`, `src/features/common/MediaGate.tsx`, and `src/features/post/ReplyLimitedNotice.tsx` (all moved verbatim and imported back, no behavior change; the single unused `EyeOff` lucide import was dropped). The pure moderation-label helpers (`moderationLabelText`, `isSensitiveLabel`, `sensitiveMediaValues`) moved with them into `src/lib/moderation.ts`. Added 19 behavioral tests (#19): `src/lib/moderation.test.ts` (10 unit cases) plus React Testing Library smoke suites for each extracted component (`Avatar` 3, `State` 2, `MediaGate` 3, `ReplyLimitedNotice` 1). No regex guardrails matched these six components, so none were retired this slice. Full gate green: vitest 237 tests / 18 files, `tsc --noEmit`, `npm run build` (static-output audit + all verifiers); live smoke-verified via dev server + CDP: shell + Discover feed render post cards with `.avatar` images, no console errors. Next candidates: `ExternalLinkCard`, `PostCard`, `PostComposer`, `ThreadView`, `ImageViewer`, `DevInspector`.

Done in the 2026-08-01 decomposition slice 3 (issues #18/#19): extracted the remaining small presentational cluster — `BackToTopButton`, `ToastHost` (plus the `ToastKind`/`ToastMessage` types, which App now imports from it), `ProfileContextPanel`, and the `RateLimitState`/`EmptyState`/`EndOfFeedCard` state cards — out of `src/App.tsx` into `src/features/feed/BackToTopButton.tsx`, `src/features/common/ToastHost.tsx`, `src/features/rightRail/ProfileContextPanel.tsx`, and `src/features/common/State.tsx` (all moved verbatim and imported back, no behavior change; the now-unused `scrollFeedToTop` App import was dropped, `ChevronUp`/`X`/`RefObject` remain used elsewhere in App). Added 12 behavioral tests (#19): `State.test.tsx` grew `RateLimitState`/`EmptyState`/`EndOfFeedCard` cases (7 total in the file), new `ToastHost.test.tsx` (3), `BackToTopButton.test.tsx` (2, mocking `readScrollOffset`/`scrollFeedToTop` from `../../lib/scroll`), and `ProfileContextPanel.test.tsx` (3). No regex guardrails matched any of these components, so none were retired this slice. Also enabled `globals: true` in the vite test config so `@testing-library/react` registers its `afterEach` cleanup — the RTL suites previously leaked rendered DOM between tests in a file (latent; fixed with this slice's multi-render suites). Full gate green: vitest 250 tests / 21 files, `tsc --noEmit`, `npm run build` (static-output audit + all verifiers); live smoke-verified via dev server + CDP: shell + Discover feed render clean (no console errors), `BackToTopButton` appears after scrolling past 600px and jumps back to top + hides on click, and the right-rail `ProfileContextPanel` renders real profile data on `/profile/bsky.app`. Next candidates: `ExternalLinkCard`, `PostCard`, `PostComposer`, `ThreadView`, `ImageViewer`, `DevInspector`.

Done in the 2026-08-01 decomposition slice 4 (issues #18/#19): extracted the post-card leaf utilities out of `src/App.tsx` into `src/features/post/` — `renderRichText` → `RichText.tsx` (the render-mapping half of rich text; `segmentRichText` stays pure in `src/richtext.ts`), `ExternalLinkCard` + `formatExternalUrlLabel` → `ExternalLinkCard.tsx`, `UnsupportedEmbedNotice` + `formatUnsupportedEmbedType` → `UnsupportedEmbedNotice.tsx`, `useReplyGate` → `useReplyGate.ts`, and the canonical `postBskyUrl` moved into `src/lib/url.ts` (typed structurally so the pure lib does not import `api.ts`). All moved verbatim and imported back (dropped App's now-unused `segmentRichText` and `ReactNode` imports; `getExternalEmbed`/`safeHttpUrl`/`RichTextFacet` remain used in App). Added 20 behavioral tests (#19): `RichText.test.tsx` (5 — link anchor + stopPropagation, mention click → profile, tag click → tag search, safe-URL downgrade, plain-text passthrough), `ExternalLinkCard.test.tsx` (7 — title/host/description/thumbnail, hide-thumbnail, no-thumb collapse, unsafe-uri null, `formatExternalUrlLabel`), `UnsupportedEmbedNotice.test.tsx` (4 — notice + bsky.app link, generic namespace, `formatUnsupportedEmbedType`), `useReplyGate.test.ts` (3 — reply-disabled notice, permitted reply, URI-keyed reset), plus `postBskyUrl` cases in `src/lib/url.test.ts` (2). Retired three regex guardrails in `scripts/verify-reader-behavior.mjs` (the `segmentRichText` App import, the `renderRichText` definition, and the `TagSearchContext`+`onOpenTag` render pattern) — their guarantees are now covered by the executable `verify-richtext.mjs` harness plus the new `RichText` suite (per the #19 migration plan). Remaining regex tally: 68 in `verify-reader-behavior.mjs` + 44 in `verify-layout-behavior.mjs`. **Constraint learned this slice:** the PostCard/media cluster (`PostCard`, `PostEmbeds`, `QuotedPostCard`, `PostImageVideoMedia`, `VideoEmbedCard`, `MediaOnlyPostCard`, …) canNOT be extracted yet because `scripts/verify-layout-behavior.mjs` pins its App.tsx source (the `image.aspectRatio` / `video.aspectRatio` / `videoFrameStyle` regexes) to `src/App.tsx`, and that script carries the in-flight #7 CSS-token edits (untouched per the worktree rule). The `ExternalLinkCard` move (no matching layout regex) is the safe boundary this slice; the big media-cluster extraction must wait until #7 lands and the layout-script regexes can be swapped to behavioral tests. Full gate green: vitest 270 tests / 25 files, `tsc --noEmit`, `npm run build` (static-output audit + all verifiers); live smoke-verified via dev server + CDP: shell + Discover feed render with `.link-card` (2) and `.post-tag` (1) present, no console errors.

Done in the 2026-08-01 decomposition slice 5 (issues #18/#19): extracted the remaining right-rail panels and the last small presentational components out of `src/App.tsx` into `src/features/` — `RecentPanel` + the `RecentItem` type → `src/features/rightRail/RecentPanel.tsx`, `FeedContextPanel` + the `EntityCache` type → `src/features/rightRail/FeedContextPanel.tsx`, `TrendingPanel` → `src/features/rightRail/TrendingPanel.tsx`, `PinnedSearchesPanel` → `src/features/rightRail/PinnedSearchesPanel.tsx`, `PinnedProfilesPanel` → `src/features/rightRail/PinnedProfilesPanel.tsx`, `HomeSourcePicker` + the `HomeOption` type → `src/features/feed/HomeSourcePicker.tsx`, `DiscoverFeedCard` → `src/features/feed/DiscoverFeedCard.tsx`, and `ProfileDetailHeader` + the `profileTabs` const + `ProfileTab` type → `src/features/profile/ProfileDetailHeader.tsx`. All moved verbatim and imported back into App (dropped App's now-unused `Check` lucide import; `useResetTimeout` was hoisted to a shared `src/features/common/useResetTimeout.ts` since both App and the extracted panels/header use it). Added 35 behavioral tests (#19): `RecentPanel` 4, `FeedContextPanel` 4, `TrendingPanel` 4 (mocking `getTrendingTopics` for the live/fallback/default paths), `PinnedSearchesPanel` 3, `PinnedProfilesPanel` 3, `HomeSourcePicker` 6 (grouped popover, filter-as-you-type, commit, sign-in hint; note `getByText("Following")` matches the group heading AND the option label — scope to `.home-picker-group` or the listbox when asserting), `DiscoverFeedCard` 5, `ProfileDetailHeader` 6 (tabs incl. Feeds/Lists, New-post, pin, optimistic follow/unfollow). Retired three reader regex guardrails: the `TrendingPanel` getTrendingTopics definition and the two `profileTabs` source-text assertions (Feeds/Lists tab coverage now lives in `ProfileDetailHeader.test.tsx`). Remaining regex tally: 65 in `verify-reader-behavior.mjs` + 44 in `verify-layout-behavior.mjs`; App.tsx now 10,702 lines (down from ~11,400 across slices). jsdom gotcha this slice: `Element.prototype.scrollIntoView` is not implemented — the `HomeSourcePicker` suite stubs it in a `beforeAll`. Full gate green: vitest 305 tests / 33 files, `tsc -b`, `npm run build` (static-output audit + all verifiers); live smoke-verified via dev server + CDP: shell renders with `.recent-panel` + `.trending-panel` on the feed, `FeedContextPanel` on `/feed/`, `HomeSourcePicker` in Settings, `ProfileDetailHeader` with all six tabs on `/profile/`, no console errors. Next: `PostComposer`, `ThreadView`, `ImageViewer`, `DevInspector`, and the `useRef<Record>` cache layer (blocked media cluster still waits on #7).

Done in the 2026-08-01 decomposition slice 6 (issues #18/#19): extracted the three remaining mid-size presentational components out of `src/App.tsx` — `ThreadEngagementPanel` → `src/features/post/ThreadEngagementPanel.tsx` (the on-demand likes/reposts/quotes panel), `DevInspector` → `src/features/rightRail/DevInspector.tsx` (the DEV-only right-rail metrics panel), and `ImageViewer` + the shared `ImageViewerImage`/`ImageViewerState` types → `src/features/post/ImageViewer.tsx` (the ~560-line media lightbox with pinch/pan/zoom/thumbnail/keyboard nav; the types are exported and re-imported by App since ~20 App call sites use them). To make ThreadEngagementPanel import-clean, the four API error-classification helpers moved from App into `src/api.ts` (`isRateLimit`, `isNetworkError`, `isUpstreamFailure`, `rateLimitMessage`) — they are error-classification over `ApiError`/XRPCError status, so api.ts is the natural home; App now imports them from `./api` and dropped its local copies. All components moved verbatim and imported back into App (dropped App's now-unused `ApiError`, `Info`, `ReactPointerEvent`, `useLayoutEffect` imports; `getLikes`/`getRepostedBy`/`getQuotes` are no longer imported by App since only ThreadEngagementPanel calls them). Added 27 behavioral tests (#19): `ThreadEngagementPanel` 9 (loading→actors for likes/reposts/quotes, empty states, rate-limit surfacing, load-more pagination + error retention, profile nav, close), `DevInspector` 5 (metric rows, feed-label vs route-kind source label, formatted counts + joined warnings), `ImageViewer` 10 (alt text, image X of Y, thumbnails + nav, arrow-key + wrap-around nav, Escape + close button, info-footer toggle, open-original link, no-alt fallback), plus 3 `isRateLimit`/`isUpstreamFailure`/`rateLimitMessage` unit cases in `api.test.ts`. Retired four reader regex guardrails (the ThreadEngagementPanel on-demand load def, the cursor-pagination def, the generic engagement loadMore, and the load-more button) — their guarantees now live in `ThreadEngagementPanel.test.tsx`. Remaining regex tally: 64 in `verify-reader-behavior.mjs` + 44 in `verify-layout-behavior.mjs`; App.tsx now 9,845 lines. jsdom gotchas this slice: `window.matchMedia` is not implemented — the `ImageViewer` suite stubs it in a `beforeAll` (the component reads it to decide whether the info footer starts visible). Full gate green: vitest 332 tests / 36 files, `tsc -b`, `npm run build` (static-output audit + all verifiers); live smoke-verified via dev server + CDP: shell + Discover feed render clean, the DEV-only `.dev-inspector` shows its 8 metric rows in the right rail, opening a thread post renders `.thread-view` + 3 `.thread-node`s, clicking the Reposts stat opens the extracted `ThreadEngagementPanel` with 50 loaded actor cards (close works), and clicking a feed image opens the extracted `ImageViewer` with the open-original link (Escape closes). Next: `PostComposer` (still the big one), `ThreadView`, the `useRef<Record>` cache layer, and the media cluster once #7 lands (blocked media cluster still waits on #7; the layout-verifier media regexes are still pinned to App.tsx).

Done in the 2026-08-01 decomposition slice 7 (issues #18/#19): extracted the full composer cluster out of `src/App.tsx` into `src/features/composer/PostComposer.tsx` — `PostComposer` (the unified new-post/reply composer), `PostLanguagePicker` + the `postLanguageStorageKey`/`postLanguageHistoryStorageKey`/`POST_LANGUAGE_HISTORY_LIMIT`/`ISO_639_1_CODES`/`languageDisplayName`/`POST_LANGUAGE_OPTIONS`/`readDefaultPostLanguage`/`postLanguageLabel`/`readPostLanguageHistory`/`recordPostLanguage` language cluster, `EmojiPicker` + `EMOJI_GROUPS`, `readReplyDefaultLanguage`, and the `ComposerImageState` type. All moved verbatim and imported back into App; App re-imports the shared bits it still uses (`PostComposer`, `POST_LANGUAGE_OPTIONS` + `languageDisplayName` for the Settings content-language selector, `composerDraftStorageKey` for `readComposerDraft`, and the `PostRefValue` type — App's local copies of `PostRefValue`/`composerDraftStorageKey`/`replyDraftPrefix` were removed; `replyDraftPrefix` moved wholly into the composer module). Dropped App's now-unused lucide `Image`/`Smile` imports and the `POST_BYTE_LIMIT`/`POST_GRAPHEME_LIMIT`/`graphemeLength`/`splitTextForThread`/`utf8ByteLength`/`MAX_POST_IMAGES`/`publishPost`/`publishThread` imports (all now only used by the composer module). Added 17 behavioral tests (#19): `src/features/composer/PostComposer.test.tsx` — new-post collapse/expand + badge, controlled-draft editing + autosave/clear of the localStorage draft, remaining-character count, multi-post split count, disabled-without-content + thread publish + re-collapse, "Post thread" labeling, publish-error surfacing, reply inline frame + parent preview, canReply=false gate, reply publish with root/parent refs + onReplied/onClose, per-thread reply-draft persistence + post-publish clearing, and over-limit reply blocking. No reader/layout regexes matched the composer cluster, so none were retired this slice (tally stays 64 in `verify-reader-behavior.mjs` + 44 in `verify-layout-behavior.mjs`); App.tsx now 9,035 lines (down from 9,845). jsdom gotchas this slice: `window.requestAnimationFrame` (stubbed — `insertAtCaret` uses it to restore caret after emoji insert) and `URL.createObjectURL`/`revokeObjectURL` (stubbed — image attachment creates blob URLs), and the controlled new-post draft requires a stateful `Harness` wrapper (a stubbed `onDraftChange` never updates `hasContent`, so publish/disabled assertions must go through the harness). Full gate green: vitest 349 tests / 37 files, `tsc -b`, `npm run build` (static-output audit + all verifiers); live smoke-verified via dev server + CDP: shell + Discover feed render clean with 4 post cards and no console errors (the composer itself is signed-in gated and does not render in the localhost preview, as expected). Next: `ThreadView`/`renderThreadNode` + `CombinedThreadViewCard`/`LongThreadCard` cluster (reader regexes for `threadUnavailableState` + `.thread-alert` live there — retire as you move), then the `ExploreDiscoverFeeds`/`ExploreTrendingTopics`/`ProfileFeedsTab`/`ProfileListsTab` surfaces, then the `useRef<Record>` cache layer (item 2 of #18), and the media cluster once #7 lands.
Done in the 2026-08-01 decomposition slice 14 (issues #18/#19): extracted the Explore + profile-tab surface cluster out of src/App.tsx into src/features/ -- ExploreTrendingTopics -> src/features/explore/ExploreTrendingTopics.tsx (new features/explore/ dir for the Explore surfaces), ExploreDiscoverFeeds -> src/features/explore/ExploreDiscoverFeeds.tsx, ProfileFeedsTab -> src/features/profile/ProfileFeedsTab.tsx, and ProfileListsTab + its shared listPurposeLabel helper -> src/features/profile/ProfileListsTab.tsx (App re-imports listPurposeLabel for the BlueskyListCard Lists-workspace card). All moved verbatim and imported back (dropped App's now-unused getActorFeeds/getActorLists/getTrendingTopics/TrendingTopic and DiscoverFeedCard imports; getPopularFeedGenerators stays since App's loadFeedSearch still uses it). The ThreadView cluster stays put because it depends on PostCard/PostEmbeds (the #7-blocked media cluster -- importing them back would be a circular dep). Added 28 behavioral tests (#19): ExploreTrendingTopics.test.tsx (4), ExploreDiscoverFeeds.test.tsx (5), ProfileFeedsTab.test.tsx (6), ProfileListsTab.test.tsx (7, incl. listPurposeLabel unit cases). Retired twelve reader regex guardrails in scripts/verify-reader-behavior.mjs (the four function defs + their loadPage/loadMore/refetch pins and the isCurateList curated-list-open rule; the profile-tab call-site pins and api-export checks stay since App owns the tab switch). Remaining regex tally: 52 in verify-reader-behavior.mjs (down from 64) + 44 in verify-layout-behavior.mjs; App.tsx now 8,605 lines (down from 9,035). jsdom gotchas this slice: mock call-counts accumulate across tests in a file (add beforeEach mockReset) and listPurposeLabel output shares one small node with the member count (assert against the .discover-feed-body textContent, not getByText). Full gate green: vitest 371 tests / 41 files, tsc -b, npm run build (static-output audit + all verifiers); live smoke-verified via dev server + CDP: Explore route renders .trending-topics with 38 topic chips, /feeds renders ExploreDiscoverFeeds with 15 .discover-feed-cards, a public profile's Feeds tab renders 7 published feeds and its Lists tab renders a curated list (User list . 21 members) -- no console errors. Next: the ThreadView/renderThreadNode + CombinedThreadViewCard/LongThreadCard cluster (still blocked on the media cluster / #7), the useRef<Record> cache layer (item 2 of #18), the CSS-token + dead-selector sweep (#20) once #7 lands, and the media-cluster extraction after that.
Done in the 2026-08-01 session (#25, closed): extracted the triplicated outside-click/Escape dismiss wiring from the composer pickers (PostLanguagePicker, EmojiPicker) and HomeSourcePicker into a shared useDismissMenu(rootRef, open, onClose, onEscape?) hook in src/features/common/useDismissMenu.ts (callbacks held in refs so the effect stays keyed on open like the originals). Co-located RTL suite useDismissMenu.test.tsx (4 tests: outside-click closes, inside-click does not, Escape + onEscape hook, listeners only while open). No behavior change; gate green (375 tests).

Done in the 2026-08-01 decomposition slice 15 (issue #18 item 2): pulled the App's nine `useRef<Record<string, T>>` data caches (feed, feedMetadata, listMetadata, profile, search, actorSearch, feedSearch, thread, threadBranch) behind a real cache layer — `src/lib/cache.ts` provides a `Cache<T>` interface (`get`/`set`/`has`/`delete`/`clear`/`keys`/`entries`/`size`), a plain `createCache<T>()` factory (Map-backed, seeds from an optional entries record), and a `useCache<T>(initializer?)` hook that returns a stable per-component-instance cache (created once in a ref, so effect/callback reads don't retrigger renders — same semantics as the old mutable Record refs, now behind one abstraction that a query layer such as TanStack Query could later slot in behind). App's nine refs became `useCache<...>()` and every one of the 39 read/write/delete/clear/iterate call sites was migrated to the Cache API, including the auth-wipe effect, `handleSignOut`, `removePostFromState`, `reloadCurrentProfile`, `invalidateOwnContentCaches`, and the thread branch loader. The load-bearing ORDERING CONTRACT comment (cache keys carry no viewer DID, so the auth-wipe effect must stay declared above the loaders) is preserved verbatim. The `scrollCacheRef`/`scrollAnchorCacheRef` caches were deliberately NOT migrated — `scripts/verify-reader-behavior.mjs` pins `scrollCacheRef.current[...]` source text and those two are sessionStorage write-through mirrors of the restore machinery, not loader caches. Added `src/lib/cache.test.tsx` (9 tests: createCache empty/round-trip/overwrite/delete/clear/seed, useCache stable-across-renders + initializer-once + per-instance isolation). No reader/layout regexes matched the nine caches, so none were retired this slice (tally stays 52 in verify-reader-behavior.mjs + 44 in verify-layout-behavior.mjs). Full gate green: vitest 384 tests / 43 files, `tsc -b`, `npm run build` (static-output audit + all verifiers); live smoke-verified via dev server + CDP: shell + Discover feed render 4 post cards, cache hits counted 2 across a feed switch-and-return, no console errors. Follow-up filed (#27): migrate the scroll caches to the cache layer + centralize the three duplicated nine-cache clear sequences once the scrollCacheRef regexes are swapped for behavioral tests.

Done in the 2026-08-01 session (#26, closed): deduplicated the hand-rolled cursor-pagination state machine shared by ProfileFeedsTab and ProfileListsTab into `useCursorPaged<T>(loadPage)` in `src/features/common/useCursorPaged.ts` (initial page load + reset on `loadPage` identity change, abort-on-teardown, load-more re-entrancy guard, error retention on load-more failure, and a `reset()` that re-runs the effect via a tick). Both profile tabs now call it (`loadPage` returns `{ items, cursor }`; render from `state.items`), dropping each tab's duplicated `useState`/`loadMoreBusyRef`/`loadMoreControllerRef`/`useEffect`/`loadMore` block (~70 lines). Co-located suite `useCursorPaged.test.tsx` (7 tests: mount load, loadMore append, first-page error + rate-limit classification, load-more failure retention + recovery, reset refetch, loadPage-identity refetch). No regex guardrails matched, so #19 tally unchanged (52/44). Full gate green: vitest   391 tests / 44 files, `tsc -b`, `npm run build` (static-output audit + all verifiers); live smoke-verified via dev server + CDP: profile Feeds tab renders 7 feed cards and Lists tab renders its curated list (".discover-feeds" aria-labels correct), no console errors. Optional follow-up for a future surface that needs the same pagination: fold in this hook rather than re-adding listeners.

Done in the 2026-08-01 session (#27 item 2): centralized the three duplicated nine-cache `clear()` sequences in `src/App.tsx` into one `clearAllDataCaches()` (`useCallback`, declared right above the auth-wipe effect so the ORDERING CONTRACT comment still holds — the cache instances are stable, so effects and callbacks can call it freely). All three sites now call it: the auth-wipe effect (signed-in DID change), `clearLocalReaderData` (Settings clear-data / sign-out wipe), and `removePostFromState` (post delete; note it previously cleared only the five post-bearing caches — feed/profile/search/thread/threadBranch — and now clears all nine, a conservative invalidation that just refetches metadata). No reader/layout regexes matched the clear sequences, so the #19 tally is unchanged (52/44). Full gate green: vitest 391 tests / 44 files, `tsc --noEmit`, `npm run build` (static-output audit + all verifiers). #27 stays OPEN for its items 1 (scroll-cache migration, blocked on the `scrollCacheRef` regex swaps under #19) and 3 (loader extraction).

Done in the 2026-08-01 session (#28, closed): extracted the data loaders out of `src/App.tsx` into a new `src/lib/loaders.ts` so the loader state machines can be tested without mounting App. The module is a set of factory functions — `createFeedLoader`, `createProfileFeedLoader`, `createSearchLoader`, `createActorSearchLoader`, `createFeedSearchLoader`, `createThreadLoader`, `createThreadBranchLoader`, `createFeedMetadataLoader`, `createListMetadataLoader` — each closing over the caches, state setters, and scroll helpers it needs (the exact closure of the old App `useCallback`s). App now builds each loader with `useCallback(createXLoader({...}), [deps])` (deps preserved: `loadFeed` re-created on `[density, signedInDid]`, the rest on `[]`), so the ORDERING CONTRACT (auth-wipe effect declared above the loader effects) and the loader identity semantics are unchanged. The loaders' shared state types (`FeedState`/`SearchState`/`ActorSearchState`/`FeedSearchState`/`DevMetrics`/`BranchLoadResult`/`ProfileFeedFilter`/`ThreadState`), the empty-state constants, `initialDevMetrics`, and the thread-hydration helpers (`hydrateThreadContinuations`/`mapWithConcurrency`/`hydrateProfileSelfThreads`/`postHasVisualMedia`/`countVisualFeedItems` + `MEDIA_DENSITY_*`) also moved into the module (App re-imports `hydrateProfileSelfThreads`/`postHasVisualMedia` for VirtualPostList and the Bookmarks surface). The two metadata effects became factory-built `loadFeedMetadata`/`loadListMetadata` loaders. Retired three reader regex guardrails in `scripts/verify-reader-behavior.mjs` per #19 (the `restoreScrollFor(cacheKey)` call-site, the `cursor ? { ...status: "ready", loadMoreError: rateLimitMessage(error) }` load-more error retention, and the `isListUri(...) ? await getListFeed(...)` list-URI routing — their guarantees now live in `src/lib/loaders.test.ts`). Remaining regex tally: 45 in `verify-reader-behavior.mjs` (down from 48) + 44 in `verify-layout-behavior.mjs`; App.tsx now ~7,900 lines (down from 8,565). Added `src/lib/loaders.test.ts` (26 behavioral tests: cache-hit vs fresh-load for every loader, list/following/authed feed routing, load-more append + error retention, rate-limit classification, abort no-mutation, allSettled profile/feed separation + public-fallback, thread ready/error/abort-prior, branch cached/fetch/error/no-op guard, metadata cache-hit/fetch/clear). Vitest gotcha: a describe-level `beforeEach(() => mock.mockReset())` combined with `mockImplementation(() => Promise.reject(...))` makes vitest's jsdom attribute the mock rejection as an unhandled rejection and fail the test even though the loader's `.catch` consumes it — reset the mock inside the specific test that asserts not-called (the branch "no-ops" test) instead of a describe-level reset. Full gate green: vitest 432 tests / 46 files, `tsc --noEmit`, `npm run build` (static-output audit + all verifiers). Follow-up (#28 now CLOSED): the optional loader-extraction is done; next App.tsx decomposition targets remain the ThreadView + PostCard/media clusters (both still blocked on #7) and the mega-stylesheet split.

Also resolved 2026-08-01 (#4, closed): `looksLikeOAuthCallback` in `src/auth.ts` no longer trips on a stray `#state=…&error=…` fragment. The original suggestion (restrict to `location.search`) would have broken sign-in — BigBsky uses the atproto SDK's default `"fragment"` response mode (no `response_mode` in the hosted or loopback client metadata), so callback params land in the hash. The fix mirrors the SDK's `findRedirectUrl()` gate: the function now also requires the pathname to be a redirect-URI path (`/oauth/callback` in production, `/` on a loopback origin). 9 behavioral tests added in `src/auth.looks-like-callback.test.ts` (fragment/error/query callbacks, stray-fragment false negatives on feed/profile routes, loopback-root detection, missing state or code/error). Gate green: vitest 400 tests / 45 files, `tsc`, `npm run build`.

Done in the 2026-08-01 decomposition slice 16 (issues #18/#19): extracted the Lists surface cluster out of `src/App.tsx` into `src/features/lists/ListsSurface.tsx` — `ListsSurface`, `BlueskyListCard`, `ListMemberManager`, and the `listToFeedSource`/`listBskyUrl` helpers, plus the `LocalList` type (App re-imports `ListsSurface` + `LocalList`; the cluster imports the shared `listPurposeLabel` from `features/profile/ProfileListsTab`). Because `BlueskyListCard` surfaces toasts via `useContext(ToastContext)`, the `ToastContext` definition hoisted from App into `src/features/common/ToastHost.tsx` (next to the `ToastKind`/`ToastMessage` types it already owns) and App imports it back. All moved verbatim and imported back (dropped App's now-unused `FormEvent`, `ListMember`, `addAccountToList`, `getListMembers`, `muteList`, `removeListItem`, `subscribeBlockList`, `unsubscribeBlockList`, `unmuteList`, and `listPurposeLabel` imports; `getMissingScopes` stays since App's reauth banner + notifications still call it). Added 14 behavioral tests (#19) in `src/features/lists/ListsSurface.test.tsx`: BlueskyListCard renders name/purpose/member-count/description, opens a curated list in-app, keeps modlists on the Open-on-Bluesky link, surfaces an error toast on failed owned-list delete, hides delete for others' lists, re-syncs block-list subscription state from `viewer.blocked`, subscribes to a modlist as a block list; ListMemberManager loads members + add-by-handle refresh; ListsSurface signed-out prompt, loading, owned/subscribed sections, error+retry, and the browser-collections toggle. Retired two reader regex guardrails in `scripts/verify-reader-behavior.mjs` (the list-delete error-toast rule and the BlueskyListCard `viewer.blocked`/`viewer.muted` re-sync rule — both guaranteed by the new suite). Remaining regex tally: 43 in `verify-reader-behavior.mjs` (down from 45) + 44 in `verify-layout-behavior.mjs`; App.tsx now 7,565 lines (down from 8,052). Full gate green: vitest 446 tests / 47 files, `tsc --noEmit`, `npm run build` (static-output audit + all verifiers); live smoke-verified via dev server + CDP: `/lists` renders the Lists placeholder + signed-out "Sign in to see your lists" empty state + browser-collections toggle, the home feed still renders 4 post cards, zero console errors. Next: the ThreadView + PostCard/media clusters (both still blocked on #7) and the mega-stylesheet split.

Done in the 2026-08-01 decomposition slice 17 (issues #18/#19): extracted the notifications cluster out of `src/App.tsx` into `src/features/notifications/NotificationsSurface.tsx` — `NotificationsSurface`, `AuthedNotifications`, and the `notificationReasonText` const (only `NotificationsSurface` is exported; the other two stay module-local). Two shared bits moved with it and are re-imported by App: `formatPostTime` moved into `src/lib/time.ts` (a pure time formatter, sibling to `postSortAt`), and the `AuthState` type moved into `src/auth.ts` next to `AuthSnapshot` (both were App-local before). App's now-unused `getNotifications`/`markNotificationsSeen`/`NotificationItem` imports were dropped (`getMissingScopes` stays — the reauth banner still calls it). Added 13 behavioral tests (#19) in `src/features/notifications/NotificationsSurface.test.tsx`: signed-out summary + open-search action, loading state, notification rows with reason text/handle/preview, unread-row class, follow→profile vs reply→post navigation, like/repost→subject-post routing with the self handle, Mentions-tab filtering, empty state, cursor pagination via Load more, load-more error retention with Retry, missing-scope reauthorize, generic error + Retry, and pinned-summary sorting + pin toggle. No verify-script regexes matched the moved cluster (checked `rg "notification|formatPostTime|AuthState" scripts/verify-*.mjs` first), so none were retired — the reader tally stays 43 + 44 layout. App.tsx now 6,984 lines (down from 7,565). Full gate green: vitest 465 tests / 48 files, `tsc --noEmit`, `npm run build` (static-output audit + all verifiers); live smoke-verified via dev server + CDP: `/notifications` renders the signed-out placeholder + 6 local reader-summary items + sign-in form, zero console errors. Next: the ThreadView + PostCard/media clusters (both still blocked on #7) and the mega-stylesheet split.

Done in the 2026-08-01 decomposition slice 18 (issues #18/#19): extracted the last small App-local leaf components out of `src/App.tsx` into `src/features/` — `FeedDensityOverrideControl` + `FeedShowMediaOverrideControl` + the `densityModes` const + `DensityMode` type → `src/features/feed/FeedDensityControls.tsx` (App re-imports all four, dropping its local `densityModes`/`DensityMode` definitions), `SignInForm` + `AccountPanel` → `src/features/auth/AccountPanel.tsx` (new `features/auth/` dir), and `SearchBox` → `src/features/search/SearchBox.tsx` (new `features/search/` dir). All moved verbatim and imported back — the `AuthState` type they need already lives in `src/auth.ts`. Added 21 behavioral tests (#19): `FeedDensityControls.test.tsx` (11 — default label, override commit/clear, media-paused option disable + warning, all modes offered), `AccountPanel.test.tsx` (8 — signed-in identity + sign-out, avatar, signed-out/checking notes, error-warning class, entered-handle submit, busy-button disable, form submit), and `SearchBox.test.tsx` (4 — submit current value, onChange edits, clear-button visibility/clear, empty hides clear). No verify-script regexes matched any of these components (checked `rg "SignInForm|AccountPanel|SearchBox|FeedDensity|FeedShowMedia|feed-density-control|sign-in-form|search-box" scripts/verify-*.mjs` first), so none were retired — the reader tally stays 43 + 44 layout. App.tsx now 7,141 lines (down 185 from the 7,326 pre-slice state). Full gate green: vitest 486 tests / 51 files, `tsc -b`, `npm run build` (static-output audit + all verifiers); live smoke-verified via dev server + CDP: home renders `.search-box` + `.account-panel` + `.sign-in-form`, `/feeds` renders the 4 `.feed-density-control` selects on the two built-in feed cards, `/search` renders its `.search-box`, zero console errors. Next: the ThreadView + PostCard/media clusters (both still blocked on #7) and the mega-stylesheet split.

Done in the 2026-08-01 decomposition slice 19 (#31, closed): co-located the four feed-density/feed-media preference helpers still living in `src/App.tsx` (`feedPreferenceKey`/`feedPreferenceKeys`/`feedDensityOverride`/`feedShowMediaOverride`) into `src/features/feed/FeedDensityControls.tsx`, next to the controls and `densityModes`/`DensityMode` they serve (the module now imports `feedSources` from `../../sources` to resolve known-feed id aliases). App re-imports all four from the module and dropped its local copies — call sites (`densityKey`, route density/showMedia override computation, `updateFeedDensityOverride`/`updateFeedShowMediaOverride`, and both feed-directory card renders) are unchanged. Added 7 unit cases (#19) to `FeedDensityControls.test.tsx` (uri key, id + known-feed alias resolution, no-override undefined, valid/invalid density override, boolean showMedia, non-boolean ignored). No verify-script regexes matched the moved helpers (checked `rg "feedPreferenceKey|feedDensityOverride|feedShowMediaOverride|FeedDensity|feed-density" scripts/verify-*.mjs`), so the #19 tally stays 43 reader + 44 layout. Full gate green: vitest 495 tests / 51 files, `tsc --noEmit`, `npm run build` (static-output audit + all verifiers). App.tsx now 7,119 lines. Next: the ThreadView + PostCard/media clusters (both still blocked on #7) and the mega-stylesheet split.

Done in the 2026-08-01 decomposition slice 20 (#19, this session): co-located the pinned-feed-meta cluster out of `src/App.tsx` into `src/lib/feed-meta.ts`, next to the `isPinnedFeedMeta` validator it already owned — the `pinnedFeedMetaStorageKey`/`pinnedFeedsStorageKey` keys, `readPinnedFeedMeta()`, `readPinnedFeedIds()` (resolves pinned ids against both static `feedSources` and the persisted discovered-feed metadata), and a new `writePinnedFeedMeta()` used by App's `togglePinnedFeed` (App re-imports all four and dropped its local consts/functions). Added 9 behavioral tests (#19) to `src/lib/feed-meta.test.ts` (read round-trip, write-overwrite, malformed-entry filtering, non-JSON fallback, 12-entry cap, knownIds resolution against static + meta sources, meta-source default read, non-JSON id fallback). Retired four reader regex guardrails in `scripts/verify-reader-behavior.mjs` (the `pinnedFeedMetaStorageKey` pin, the `readPinnedFeedMeta` definition pin, the `knownIds` resolution pin, and the `setPinnedFeedMeta` write-path pin) — their guarantees now live in the behavioral suite. Reader tally 43 → 39 + 44 layout. Full gate green: vitest 506 tests / 51 files, `tsc --noEmit`, `npm run build` (static-output audit + all verifiers); live smoke-verified via dev server + CDP: pinning a built-in feed writes `bigbsky:pinned-feeds` only, pinning a discovered feed writes both keys through `writePinnedFeedMeta`, reload restores the discovered feed's metadata and the Pinned group renders — zero console errors. App.tsx now 7,094 lines. Next: keep retiring reader regexes as safe lib co-locations appear, then the ThreadView + PostCard/media clusters (both still blocked on #7) and the mega-stylesheet split.

Done in the 2026-08-01 code-review pass (#6): extracted the triplicated `handleShare` (Web Share API + clipboard fallback + AbortError handling + 1800ms transient-reset) that was copy-pasted identically in `ThreadedPostCard`, `PostActionBar`, and `CombinedThreadViewCard` into a shared `useSharePost(rootPost, posts)` hook in `src/features/common/useSharePost.ts` (returns `{ shareState, handleShare }`, uses the existing `useResetTimeout`). All three App.tsx call sites now use the hook; App dropped its now-unused `useResetTimeout` import (the hook owns it). Added `src/features/common/useSharePost.test.tsx` (6 cases: Web Share path, clipboard fallback, AbortError ignored, non-abort share failure → clipboard fallback, both-fail → error, multi-post text join). No verify-script regexes matched the share code (checked `rg "share|handleShare|navigator.share|clipboard" scripts/verify-*.mjs` first), so the #19 tally stays 39 reader + 44 layout. App.tsx now 7,006 lines (down from 7,094). Full gate green: vitest 512 tests / 52 files, `tsc -b`, `npm run build` (static-output audit + all verifiers); live smoke-verified via dev server + CDP: home feed renders 4 post cards each with a Share button and zero console errors (the share interaction itself is CDP-environment limited — `navigator.clipboard` needs document focus — so the hook behavior is covered by the unit suite). Follow-ups filed: #32 (adopt `useDismissMenu` for the PostActionBar more-menu, which still hand-rolls the outside-click/Escape wiring #25 replaced elsewhere) and #33 (`SurfaceView`'s `surfaces` map has five dead entries — `lists`/`notifications`/`profile`/`bookmarks`/`settings` all early-return before the generic fallback — plus an empty `notifications` no-op branch).

Done in the 2026-08-01 small-fix batch (#32/#33 closed + #6 pass): closed **#33** by trimming `SurfaceView`'s `surfaces` map to only the reachable keys (`explore`, `feeds`) and deleting the empty `notifications` no-op branch (its `if/else if` became a plain `if (name === "lists")`); closed **#32** by converting `PostActionBar`'s more-menu from hand-rolled `pointerdown`-capture + Escape listeners to the shared `useDismissMenu` hook (behavior already covered by `useDismissMenu.test.tsx`). Then a code-review pass over App.tsx (#6) shipped: (1) **perf fix** — `VirtualPostList`'s inline `onMeasured` arrow was recreated every scroll frame, tearing down + re-creating a `ResizeObserver` for every visible row; it is now a stable `useCallback(rowKey, height)` with the row key passed as a prop to `MeasuredPostRow` (the row-measure effect deps `[rowKey, onMeasured]` no longer churn on scroll). (2) `MediaOnlyPostCard` now calls the existing `feedViewerImages` helper instead of re-implementing the src/previewSrc/alt map+filter. (3) New module-level `feedSourceMatches(source, query)` replaces the feed-source search predicate copy-pasted in three places (feed-selector memo, pinned-group memo, SearchView local results). (4) `useSharePost.ts` exports `shareButtonLabel(state)`; the three post-card footers use it instead of a duplicated label ternary. (5) The `bigbsky:density-by-context` literal is now the `densityByContextStorageKey` const (was string-typed at 3 sites). (6) `navIcons` is now a name-keyed `Record<string, typeof Home>` (was a position-indexed array of 8 that silently misaligned with the 9 `navigationItems` — "Info" rendered a `.plain-info-icon` fallback; it now gets a real lucide `Info` icon). (7) `openNavigation("Home")` now passes `feedWaitingForAuth` into `resolveHomeSource` to mirror the active-source memo, so clicking Home during an auth check stays on the chosen Home feed instead of bouncing to Discover. No verify-script regexes matched any of these (checked `rg` first), so the #19 tally stays 39 reader + 44 layout. Full gate green: vitest 512 tests / 52 files, `tsc --noEmit`, `npm run build` (static-output audit + all verifiers); live smoke-verified via dev server + CDP: the Info nav icon renders lucide `info`, feed scroll + row measurement still work (rendered rows 4→6 on scroll, reload restores to the settled 913 offset, zero console errors), and the PostActionBar more-menu opens/closes on outside `mousedown` + Escape. Follow-ups filed from the review: #34 (dedupe `combinedThreadStats` in ThreadedPostCard/CombinedThreadViewCard), #35 (shared `PostCardHeader` across 5 card variants), #36 (orphaned `.plain-info-icon` CSS — fold into #20 sweep), #37 (dedupe `postHasEmbeds` in 4 cards). App.tsx now 6,954 lines.

Done in the 2026-08-01 dedup batch (#34/#35/#37/#44 closed + #6 pass): closed **#34** by extracting `combinedThreadStats(posts, rootPost, likeView)` (reply/repost/quote/like counts + live like-count swap + `hideThreadMarkers`, folding the duplicated "todo Bug 4" fork-count caveat) and using it in both `ThreadedPostCard` and `CombinedThreadViewCard`; closed **#37** by extracting `postHasEmbeds(post)` for the two combined cards' skip-if-empty segment check (the `PostEmbeds` `renderedEmbed` and `PostCard` `hasRichContent` predicates were deliberately left as-is - they omit `extractFacetLinks` and swapping them would change unknown-embed-notice / "no plain text" rendering); closed **#35** by extracting in-file `<PostCardHeader profile post timestampLabel onOpenProfile onOpenPost />` used by `PostCard`/`ThreadedPostCard`/`CombinedThreadViewCard`/`LongThreadCard` (~100 lines removed; the guarded `onOpenProfile && handleInternalLinkClick` pattern is behavior-neutral for the two cards with required handlers; `QuotedPostCard`'s structural `quote-header` variant left as-is); closed **#44** by adding `aria-label`s to the icon-only PostActionBar comment/like/bookmark/share buttons, the more-menu `<summary>`, and the `MediaOnlyPostCard` expand button. Then a code-review pass (#6) shipped: (1) **state-leak fix** - `ThreadView` is now keyed by route actor/rkey so the engagement panel + thread display mode reset when navigating post->post (local state previously leaked onto the new thread). (2) `toggleShowNsfw`/`toggleShowMedia` now run the `window.confirm` + localStorage writes OUTSIDE the state updater (impure; matches the codebase's persist-outside convention; the reader verifier's `safeLocalStorageSet(showNsfwStorageKey, next ? "true" : "false")` pin still matches). (3) Dropped the orphaned `danger-action` class on the Delete-post button (no such selector exists; it already inherits `.post-more-menu div button` red). (4) The feed-map button that cleared the filter was labeled "Search feeds" - now "Clear feed filter" with an `aria-label`. (5) Extracted shared `SettingsToggle` (4 settings switches) and `postTextClass` helper (4 post-text sites); dropped the now-unused `preservesLineBreaks` const. No verify-script regexes matched any of these (checked `rg` first), so the #19 tally stays 39 reader + 44 layout. Full gate green: vitest 512 tests / 52 files, `tsc --noEmit`, `npm run build` (static-output audit + all verifiers); live smoke-verified via dev server + CDP: feed boots with author-button/post-timestamp headers, `/settings` renders all four toggles and the media toggle flips + persists, zero console errors. Follow-ups filed from the review: #38 (feed-directory card body dedup), #39 (combined-thread segment + footer JSX dedup), #40 (rememberScroll per-event row scan), #41 (storage side effects inside state updaters), #42 (reply/quote toggle pair-sync dedup), #43 (media-reveal gating hook dedup). App.tsx now 6,897 lines.


Done in the 2026-08-01 batch (#46/#50/#51/#48/#39/#49/#45 closed, #6): (1) **#46** - wrapped `openTag` and `submitSearch` in `useCallback` (submitSearch deps `[navigate, remember]`, `remember` memoized on `recentItems`) so the `TagSearchContext` value is no longer recreated every App render; swapped the reader-verifier `submitSearch` pin to the new `useCallback` form (tally unchanged). (2) **#50** - `openNavigation`'s default branch now passes accurate per-surface detail strings (Lists/Bookmarks/Settings/Info) instead of "Signed-in surface placeholder". (3) **#51** - the feed-directory reorder grip is now `role="button"`/`tabIndex`/`aria-label` and each reorderable drop-target article carries an aria-label. (4) **#48** - cursor pagination for the public Feed search tab: `getPopularFeedGenerators` gained a `cursor` param, `FeedSearchState` gained `cursor`/`loadMoreError`, `createFeedSearchLoader` appends pages + retains load-more errors, App's `loadMore` handles `searchTab === "feeds"`, and SearchView renders AutoLoadMoreButton/EndOfFeedCard under the Public Feeds list; 3 new loader tests. Note: the live API returns no cursor for queried feed search (only for the unfiltered endpoint), so the button stays hidden in practice today. (5) **#39** - extracted in-file `CombinedThreadSegments` + `CombinedThreadActionBar` shared by ThreadedPostCard and CombinedThreadViewCard (the two cards' reply/quote deltas pass as props); the `ShareState` type is now imported from useSharePost. (6) **#49** - extracted in-file `MasonryImageGrid` (render-prop) deduping the PostImageVideoMedia + QuotedPostCard masonry row grid; the three count-1 single-image variants were deliberately left (they differ in class/style/behavior). (7) **#45** - the feed loader's cold-load path now restores through anchor-aware `restoreScrollFor` instead of the raw 30-frame pixel path (it falls back to the pixel path when no anchor exists); `FeedLoaderDeps` dropped the now-unused `restoreOrResetScroll`/`timelineRef`/`scrollCache` (and the `RefObject` import); App wiring + 9 test call sites updated; the fresh-load test now asserts `restoreScrollFor("feed:discover")`. No other verify-script pins matched any of this. Full gate green: vitest 523 tests / 53 files, `tsc --noEmit`, `npm run build` (static-output audit + all verifiers). Live smoke-verified via dev server + CDP: a profile feed rendered 2 combined cards (correct segments/break spans + full 9-item action bar), the feeds search tab rendered 20-21 results with the end marker, and a cold reload of a scrolled home feed restored to a stable non-zero position (anchor intra 127.8) instead of 0 - zero console errors. App.tsx is now ~7,1xx lines. Commit `493294d` pushed to main (deploy path).


Done in the 2026-08-01 small-fix batch (#47/#36 closed + #20 sweep): (1) **#47** - the mobile-header scroll effect (src/App.tsx) now keys on the full route object instead of just route.kind, so surface->surface navigation (bookmarks -> lists) re-attaches the scroll listener to the new .timeline node instead of leaking it on the detached one (route + activeSource.id + profileTab fully determine timelineRef.current). (2) **#36 + #20 sweep** - removed 11 dead selector groups from src/styles.css after a full-repo usage cross-check including dynamically-built class names: .plain-info-icon (the #36 orphan), .rail-account, .account-switcher*, .composer-draft-header, .composer-media-row, .composer-media-note, .bookmarks-list, .feed-map-panel, .local-list-timeline*, .thread-combined-body, .thread-combined-part (~290 lines). Kept .toast-error/.toast-success (built as toast-${kind}) and .thread-combined-card (live). Full gate green: vitest 523 tests / 53 files, tsc --noEmit, npm run build (static-output audit + all verifiers). Live smoke-verified via dev server + CDP: bookmarks->lists surface nav swaps .timeline with zero console errors, mobile header hide/show still works across navigation, desktop shell grid (76px 288px 1236px 320px) + mobile single column intact with no horizontal overflow after the CSS sweep. Commits 74057be + 69a1431 pushed to main (deploy path).